# Sprint S5 — Change Control

**Planned:** 2026-05-01 → 2026-05-22 (3 weeks)
**Roadmap reference:** PAFM-SD Ch 6.4, Phase 2
**Module DoD:** PAFM-SD Appendix F.6

## Sprint goal

Deliver formal change-control workflow per PAFM-SD F.6:
five-state ChangeRequest workflow (Raised → Assessed → Approved
| Rejected → Implemented → Closed), category tagging
(Scope/Time/Cost/Quality), BSA HRB categorisation tagging
(Cat A / B / C where applicable), role-gated approval, structural
links to S1 Variations + S4 programme impact, full audit trail
via the existing audit-twin pattern.

S5 is **explicitly the test for the S4-retro prediction**:
> "S5 will not close same-day, because the workflow shape is
> interpretive (what states / transitions / approval rules belong
> in v1.0?). If S5 still closes same-day, that's another data
> point to refine the cadence model."

If the spec drives a clean answer (well-formalised domain like
PMBOK CPM was in S4), the prediction falls and "novel decision"
isn't the cadence-breaker either. If the spec leaves real
ambiguity, the prediction holds.

Inherits from S0..S4:
- `ITenantContext` and the full query-filter scoping. Every new
  entity is tenant-scoped via `Project.AppointingPartyId`.
- Two-tier role model (ADR-0010): write paths gated
  `TaskTeamMember+` for raise; `ProjectManager+` or higher for
  approve / reject / implement / close transitions.
- Audit-twin pattern (PR #33) — every state transition produces
  a structured event committed atomically with the row mutation.
- `Core/<X>.cs` pure-function pattern from `Core/Cpm.cs` /
  `Core/MsProjectXml.cs` — `Core/ChangeWorkflow.cs` for the
  state-machine transition rules.
- Three-commit-per-task cadence (entity-with-service / migration
  / tests).

## Branch / repo state

`sprint/s5-change-control` is branched off
`sprint/s4-schedule-programme` rather than master, because S3 + S4
have not yet merged at S5 kickoff time. The chain is now S0 →
master ← S1 ← S2 → master, then S3 (PR #46), S4 (PR #47 stacked),
S5 (this branch, stacked on S4). Per Ch 27, this is a
deviation from "sprint branches off master" but is acceptable
when prior-sprint work is conceptually a prerequisite for the
working dev environment (the local DB has the S3 + S4 migrations
applied).

## Module-level DoD (PAFM-SD Appendix F.6 — S5 Change Control)

Verbatim from PAFM-Software-Development v2.0 `.docx` Appendix F.6:

- [x] Change request workflow — raise, assess, approve, implement, close. (T-S5-02 / T-S5-03 / T-S5-04)
- [x] Category (scope/time/cost/quality). (T-S5-02 — `ChangeRequestCategory` enum; service validates)
- [x] BSA HRB categorisation (Cat A/B/C) where applicable. (T-S5-02 — `BsaHrbCategory` enum as a tag; auto-inference deferred to v1.1 / B-037)
- [x] Delegation-based approval routing. (T-S5-04 — v1.0 simplified to flat `ProjectManager+`; per-tenant chains deferred to v1.1 / B-036, S14 territory)
- [x] Linked to variations, programme impact, cost impact. (T-S5-04 / T-S5-06 — Approve(CreateVariation=true) atomic spawn + free-text impact summaries; structural per-Activity / per-CBS-line links deferred to v1.1 / B-035)
- [x] Change register with full audit. (T-S5-04 — 6 transition audit events + audit-twin per-row; T-S5-05 register list endpoint with state / category filters)

Inherited (must continue to hold):
- Tenant isolation on every new entity.
- Audit-twin atomicity per PR #33.
- Transaction-wrap pattern on any composite write (notably the
  Approve → create-Variation step).

## Sprint-level DoD (PAFM-SD Ch 10.2)

- [x] All committed tasks closed or explicitly deferred with reason.
- [x] All tests for implemented features pass. (622/622 green at close.)
- [ ] Sprint branch merged to master. (Pending PR — user-confirmed step.)
- [ ] Build green on CI. (Pending PR — CI runs on push.)
- [x] Linter produces no new warnings (above S4 baseline). (0 warnings on `dotnet build`.)
- [x] README / ADR / code comments updated where relevant. (role-matrix.md updated per task; v1.1-backlog.md grew by 14 entries via T-S5-08 audit; no new ADRs needed.)
- [x] This Sprint Log complete through final day.
- [x] Retrospective (`docs/retrospectives/s5.md`) written.

## Task decomposition

Estimates in hours. Total multiplied by 1.5 per PAFM-SD Appendix E.

| ID | Task | Est (h) | Notes |
|---|---|---|---|
| T-S5-01 | Sprint kickoff — branch, F.6 DoD, tasks, capacity check | 1 | This commit. |
| T-S5-02 | `ChangeRequest` entity + DbContext config + per-project query filter + behavioural test | 4 | Fields: Id, ProjectId, Number ("CR-NNNN" auto, unique within project), Title, Description, Category enum (Scope/Time/Cost/Quality), BsaCategory enum (NotApplicable/A/B/C), State enum (Raised/Assessed/Approved/Rejected/Implemented/Closed), RaisedById, RaisedAt, AssessedById?, AssessedAt?, AssessmentNote, ApprovedById?, ApprovedAt?, DecisionNote, ImplementedAt?, ClosedAt?, EstimatedCostImpact (decimal?), EstimatedTimeImpactDays (int?), ProgrammeImpactSummary, CostImpactSummary, IsActive. Tenant-scoped through Project. |
| T-S5-03 | `Core/ChangeWorkflow.cs` pure-function state machine + tests | 3 | `CanTransition(from, to, role)` and `NextStates(from, role)` helpers. Pure — no DB. Mirrors `Core/CdeStateMachine` shape (S0). 10+ fixture tests covering happy paths + invalid transitions + role-gate boundary. |
| T-S5-04 | `ChangeRequestService` Raise / Assess / Approve / Reject / Implement / Close + service tests | 7 | Action names `change_request.raised` / `.assessed` / `.approved` / `.rejected` / `.implemented` / `.closed`. Each transition uses `ChangeWorkflow.CanTransition` to validate. `TaskTeamMember+` for Raise; `InformationManager+` for Assess; `ProjectManager+` for Approve / Reject / Implement / Close. `Reject` from Raised or Assessed; cannot reject after Approved. Already-Closed rejected with ConflictException. |
| T-S5-05 | Controller + role-matrix + DTOs | 2 | `POST /change-requests` (Raise), `POST /{id}/assess` (Assess), `POST /{id}/approve` (Approve), `POST /{id}/reject`, `POST /{id}/implement`, `POST /{id}/close`. Plus `GET /change-requests` (filter by state / category) and `GET /change-requests/{id}`. |
| T-S5-06 | Linked impact — Approve creates Variation + impact summaries | 3 | When approved with `CreateVariation = true`, atomically create a `Variation` (S1 entity) and emit `change_request.variation_created` audit. `ProgrammeImpactSummary` + `CostImpactSummary` are free-text in v1.0 (richer structural links to specific Activities / CBS lines deferred to v1.1 / B-035). |
| T-S5-07 | Tenant filter sweep on S5 entities | 1 | Add ChangeRequest to `CimsDbContextTenantFilterTests.TenantScoped` + behavioural data-layer filter test. |
| T-S5-08 | v1.1 backlog audit (carry-forward action from S4 retro) | 2 | Audit S0..S4 retros for "v1.1 candidate" mentions; promote to B-034..B-040 in `v1.1-backlog.md`. Promote B-027 (CI smoke test) too. Not part of F.6 strictly, but parking it here per the S4 retro action. |
| T-S5-09 | Sprint close — retrospective, tag `v0.5-sprint-s5`, merge to master | 2 | |

**Total estimate:** 25h × 1.5 = **37.5h**.

**Available sprint hours per kickoff template:**
6h × 15 working days × 80% = **72h**.

**Capacity headroom: 34.5h.** Comfortable margin — the second
sprint since S0 to fit formally without a CR (S3 and S4 also
fit, S4 only after CR-005). The headroom reflects that S5's
DoD is short (six bullets, well-formalised PMBOK / BSA territory)
and most of the supporting infrastructure (audit-twin,
role-matrix, tenant filter) is reused from S0..S4 without
modification.

**Delegation-based approval routing**: per F.6 fourth bullet.
v1.0 implementation is the simplest viable rule:
`Approve` requires `ProjectManager+` regardless of category;
`Reject` same. Richer per-tenant delegation chains
(e.g. "Cat A scope changes route to OrgAdmin; Cost ≥ £50k
routes to Sponsor") are a v1.1 / S14 Admin Console concern.
Documented as a v1.1 backlog candidate (B-036 — to be created
in T-S5-08).

## Top 3 risks (PAFM-SD Appendix E)

1. **State-machine ambiguity around the Reject / Implement /
   Close path.** PMBOK is consistent on raise / assess / approve;
   "implement" and "close" semantics vary by source. v1.0
   interpretation (documented in `Core/ChangeWorkflow.cs`
   doc-comments): Approved → Implemented when the change has
   been actioned in delivery; Implemented → Closed when the
   resulting Variation / programme update / cost adjustment
   has been recorded. Reject is allowed from Raised or Assessed
   only — once Approved, only Implemented / Closed forward.
2. **BSA categorisation legal-text drift.** The Building Safety
   Act 2022 and subsequent secondary legislation use specific
   category labels (Cat A / B / C); the exact thresholds change
   as Building Safety Regulator guidance evolves. v1.0 ships
   the categorisation as an enum *tag* with no automatic
   inference — the PM marks the CR; reporting downstream uses
   the tag. v1.1 candidate: per-tenant configurable categorisation
   rules + automatic inference from change scope.
3. **The S4-retro prediction.** S4 retro predicted S5 won't
   match the same-day cadence because the workflow shape is
   "interpretive". Mitigation: write the workflow against
   PMBOK / BSA-aligned states without litigating each
   transition; if a real interpretive decision surfaces
   mid-sprint, document it inline in
   `Core/ChangeWorkflow.cs` rather than expanding scope.

## Daily entries

### Day 1 — 2026-05-01 (kickoff)
Task today: T-S5-01 sprint kickoff.
Progress: Branch `sprint/s5-change-control` created off the
`sprint/s4-schedule-programme` tip (`f96b15d`). PAFM-SD `.docx`
Appendix F.6 extracted directly via the standing
`reference_pafm_spec.md` authorisation; six bullets verbatim.
Pre-cut estimate 25h × 1.5 = 37.5h vs the 72h formal capacity —
**34.5h headroom**, comfortably under capacity. **No CR needed.**
Commits: this commit (kickoff scaffold + F.6 backfill).
Blockers: none.
Decisions:
- Branch off S4 tip rather than master (Ch 27 deviation
  documented). S5 PR will chain S4 (which chains S3).
- Delegation-based approval routing: v1.0 ships
  `ProjectManager+ approves`, no per-tenant chains. Richer
  delegation chains → v1.1 / S14 Admin Console (B-036, to be
  created in T-S5-08).
- BSA categorisation: pure tag in v1.0, no auto-inference.
- Reject allowed from Raised or Assessed only; once Approved
  the only forward path is Implemented → Closed.
- Approve with optional `CreateVariation = true` flag spawns
  an S1 Variation atomically. Richer per-Activity / per-CBS-line
  structural links → v1.1 / B-035.
- Including the **v1.1 backlog audit** (T-S5-08) inside this
  sprint per the S4 retro action — roll all "v1.1 candidate"
  doc-comment mentions across S0..S4 into B-NNN entries plus
  promote B-027 (CI smoke test).
Drift risks noticed:
- State-machine ambiguity (Reject / Implement / Close) — Top-3
  risk #1.
- BSA categorisation legal-text drift — Top-3 risk #2.
- S4-retro prediction is the headline test for this sprint.
Tomorrow: T-S5-02 `ChangeRequest` entity + DbContext +
tenant filter + behavioural test.
Wellbeing: N/A (session-based).

### Session — 2026-05-01 (T-S5-02..08 implementation)
Task today: T-S5-02 through T-S5-08. Same compressed cadence as
S0..S4 — kickoff in the morning, full implementation chain
through the afternoon. **First sprint where the sprint-close
task itself slipped to a second calendar day** (Day 2 entry
below); the substantive code work was still same-day.

**The S4-retro prediction was tested and falsified again.** S4
predicted: "S5 will not close same-day, because the workflow
shape is interpretive (what states / transitions / approval
rules belong in v1.0?)." Result: every state-machine question
the spec posed had a textbook answer (NEC4-aligned change
control with PMBOK-canonical states). All 32 fixture tests on
`Core/ChangeWorkflow.cs` passed first try; all 18 service-level
tests on `ChangeRequestService` passed first try; all 5
CR↔Variation integration tests passed first try. Cadence model
needs another revision (see retro).

Progress (in commit order on `sprint/s5-change-control`):

T-S5-02 ChangeRequest entity:
- `3028b18` entity + 3 enums (Category, BsaHrbCategory, State)
  + DbContext + tenant filter + SetTimestamps
- `99a55db` ef migration AddChangeRequests
- `86086e3` 3 behavioural data-layer filter tests + sweep entry

T-S5-03 ChangeWorkflow state machine:
- `9a9399d` `Core/ChangeWorkflow.cs` (5-state + role gates)
  + 32 fixture tests (single commit; small task)

T-S5-04 ChangeRequestService:
- `9084866` 6 transition methods (Raise / Assess / Approve /
  Reject / Implement / Close) + 6 DTOs + DI + Approve→Variation
  spawn (T-S5-06 lite landed here for transactional atomicity)
- `77e515e` 18 behavioural tests covering full workflow lifecycle
  + role gates + audit twins + cross-tenant 404

T-S5-05 ChangeRequestsController + role-matrix:
- `748fd90` 8 endpoints under `/change-requests` + role-matrix
  documentation (single commit; thin wrapper)

T-S5-06 CR↔Variation integration:
- `a82dc2f` 5 cross-service integration tests (the spawn itself
  was already in T-S5-04's ApproveAsync)

T-S5-07 Tenant filter sweep: covered structurally by T-S5-02's
ChangeRequestFilterTests + the model-level
CimsDbContextTenantFilterTests entry. No additional commit.

T-S5-08 v1.1 backlog audit (S4-retro carry-forward action):
- `6e5f59a` 14 new B-NNN entries (B-034..B-047) covering
  doc-comment "v1.1 candidate" mentions accumulated across
  S3..S5; B-027 promoted from open-ended deferral to
  pre-pilot dedicated-infrastructure-sprint target.

T-S5-09 sprint close: this entry continuation + Day 2 entry +
retrospective + pending PR + tag.

**Test count delta:** 564 (post-S4) → 622 (close of S5). +58
tests this sprint. By far the smallest single-sprint test delta
since S2 (the cost-domain sprint with 70+ new tests) — S5's
delta is small because the workflow is structurally rote
(state machine + 6 transitions + DTOs). The substantive coverage
came from the 32 ChangeWorkflow fixture tests + 18 service
behavioural tests; everything else built on the existing
audit-twin / tenant-filter scaffold.

**Blockers:** none. Bash forking on the Windows host re-appeared
as expected; PowerShell tool used throughout per the standing
post-S0 lesson.

**Decisions made mid-sprint:**
- Approve→Variation spawn lives inside `ChangeRequestService.ApproveAsync`
  (not a separate dedicated endpoint) for transactional
  atomicity. Both audit events (`change_request.variation_created`
  + `change_request.approved`) commit with the row mutations
  via the audit-twin pattern from PR #33.
- Variation Title prefix `"From {CR.Number}: ..."` makes the
  link discoverable in the variations list view without needing
  the new structural FK.
- ChangeRequest.GeneratedVariationId is a one-way FK (CR → Variation).
  Deliberately NOT bidirectional — the Variation doesn't carry
  a `RaisedFromChangeRequestId` because it's reachable via the
  CR query, and adding the reverse FK would create a circular
  dependency the migration would need to handle.
- T-S5-08 backlog audit produced 14 new entries — one of them
  (B-035 CR ↔ Activity / CBS-line structural impact) was
  effectively pre-decided in the kickoff but only landed
  formally as a backlog entry today.

**Drift risks noticed:**
- Top-3 risk #1 (state-machine ambiguity around Reject /
  Implement / Close) — turned out not to be ambiguous. Reject
  is allowed only from Raised or Assessed; once Approved the
  forward path is Implemented → Closed. The state machine
  encodes both rules; the service layer's EnforceTransition
  helper maps invalid transitions to ConflictException with the
  from-state in the message.
- Top-3 risk #2 (BSA categorisation legal-text drift) — sidestepped
  by treating the categorisation as a manual tag in v1.0. Per-tenant
  inference rules are B-037.
- Top-3 risk #3 (S4-retro prediction) — see retro.

PR + tag: pending user confirmation per "shared-state action"
guidance. Locally on `sprint/s5-change-control` with N commits
ahead of master.

Wellbeing: N/A (session-based).

### Day 2 — 2026-05-02 (T-S5-09 sprint close)
Task today: T-S5-09 sprint close — tick DoD, append session
entry, write retrospective, prepare PR.

Progress: this entry + the session entry above + the retrospective
at `docs/retrospectives/s5.md`. The substantive S5 code work all
landed yesterday; today is the close-out admin.

**Notable as the first sprint where the calendar boundary fell
inside the sprint scope.** S0..S4 all completed kickoff-to-close
within a single calendar day. S5's substantive work is also
same-day, but the close task spans 2 days because the user took
overnight rest between the technical work and the close-out.
The S5 retrospective engages with what this means for the
cadence model.

Blockers: none.
Decisions made today: (none — pure docs / close-out).
PR + tag: as before, pending user confirmation.
Wellbeing: N/A (session-based).
