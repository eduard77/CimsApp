# Sprint S0 — Multi-tenant Foundation

**Planned:** 2026-04-24 → 2026-05-15 (3 weeks)
**Roadmap reference:** PAFM-SD Ch 6.3, Phase 1
**Module DoD:** PAFM-SD Appendix F.1

## Sprint goal

Harden the existing CimsApp stack so tenant isolation is enforced at the
data layer, audit captures actor / action / entity / before / after, and
the deployment pipeline produces a working build. Per PAFM-SD Ch 0, S0
is a refactor-and-harden sprint; the code base already runs, the goal is
to close the gaps between what's there and F.1.

## Module-level DoD (PAFM Appendix F.1)

- [x] An organisation can be created. Users are scoped to organisations.
      `POST /api/v1/organisations` (anonymous) creates an Org and mints a
      bootstrap invitation. `POST /api/v1/auth/register` requires that
      token; tenant scope is server-derived (ADR-0011, T-S0-11). Existing
      `User.OrganisationId` column unchanged.
- [x] Users can log in. Authentication works. Roles are enforced.
      JWT carries `cims:org` and `cims:role` (`f41c410` maps the latter
      to ASP.NET role). T-S0-08 audited every controller and added
      gates per ADR-0010's two-tier model.
- [x] `OrganisationId` global query filter applied across all
      tenant-scoped entities. Shipped in `3c95027`; nav-warnings cleaned
      in `e878218`. Behavioural test landed today (T-S0-04, `92ef9ff`).
- [x] Tenant context available via `ITenantContext` service.
      `65ff02c`; SuperAdmin extension in `b2a5781`; injected into
      `ProjectsService` and `OrganisationsController` for write-side gates.
- [x] Audit trail captures actor, action, entity, before/after.
      Schema in `f60389a`; SaveChanges interceptor in `d897444`;
      behavioural test in `98201e8` (T-S0-06b).
- [x] `SuperAdmin` role can see cross-tenant data (audited).
      `IgnoreQueryFilters()` opt-in path; project-creation bypass
      audited as `project.created.superadmin_bypass` (`c83a8a9`,
      ADR-0012).
- [x] Deployment pipeline produces a working build.
      GitHub Actions build+test on push (`15e8bd5`).

## Sprint-level DoD (PAFM-SD Ch 10.2)

- [x] All committed tasks closed or explicitly deferred with reason.
      T-S0-01..09, 04a, 06b, 11, 12 closed. T-S0-10 (this commit set)
      in progress.
- [x] All tests for implemented features pass.
      46/46 green locally on 2026-04-25.
- [ ] Sprint branch merged to main. (Pending T-S0-10 PR.)
- [ ] Build green on CI. (Verify after push.)
- [x] Linter produces no new warnings.
      Only the pre-existing MudBlazor 6 `Palette` deprecation warning
      remains (CS0618 in `MainLayout.razor:82`); not introduced this
      sprint, tracked for a future MudBlazor 7 upgrade.
- [x] README / ADR / code comments updated where relevant.
      ADR-0008..0012 written this sprint. Docs sweep landed across
      sprint log, role matrix, security review, change register.
- [x] This Sprint Log complete through final day.
- [ ] Retrospective (`docs/retrospectives/s0.md`) written. (This commit.)

## Task decomposition

Estimates in hours; total multiplied by 1.5 per PAFM-SD Appendix E.

| ID | Task | Est (h) | Notes |
|---|---|---|---|
| T-S0-01 | Sprint kickoff — branch, docs, DoD, tasks | 1 | This commit |
| T-S0-02 | `ITenantContext` service (interface + HTTP-context-backed impl + unit test) | 3 | Scoped per request; reads `OrganisationId` from auth claims |
| T-S0-03 | `OrganisationId` global query filter on tenant-scoped entities in `CimsDbContext.OnModelCreating` | 3 | Project, Document, DocumentRevision, CdeContainer, Rfi, ActionItem, Notification, AuditLog, ProjectTemplate; User/Organisation/RefreshToken/RfiDocument/ProjectMember/ProjectAppointment require per-case judgement |
| T-S0-04 | Integration test: tenant A cannot read tenant B's Projects / Documents | 3 | **Done 2026-04-25** (commits `0567adf` adds EF InMemory per ADR-0009, `92ef9ff` `CimsDbContextIsolationTests` + `StubTenantContext` test double). Four scenarios: own-project visibility, IgnoreQueryFilters bypass, own-user visibility, own-document visibility. |
| T-S0-05 | Migration + schema: `AuditLog.BeforeValue`, `AuditLog.AfterValue` | 2 | JSON columns `nvarchar(max)` |
| T-S0-04a | Fix filter-navigation warnings from T-S0-03 | 2 | Add matching filter on `Notification` (via User) and `RfiDocument` (via Rfi.Project). Leave `RefreshToken` unfiltered with `IgnoreQueryFilters()` at AuthService call sites. Must land before T-S0-04 behavioural test |
| T-S0-06 | EF `SaveChanges` interceptor populates `Action / Entity / EntityId / BeforeValue / AfterValue / UserId` | 6 | Covers Add/Update/Delete; reads current user from `ITenantContext`. Implementation landed 2026-04-24 (`d897444`, `f60389a`). |
| T-S0-06b | Behavioural test for `AuditInterceptor` end-to-end through `SaveChangesAsync` | 2 | **Done 2026-04-25** (commit `98201e8`). Five scenarios: Insert / Update / Delete audit-row shape, AuditLog non-recursion, anonymous-tenant skip. Uses EF InMemory (ADR-0009) + `StubTenantContext`. |
| T-S0-07 | `SuperAdmin` cross-tenant bypass via `IgnoreQueryFilters` in scoped admin queries | 3 | Audit records the bypass |
| T-S0-08 | Role enforcement audit across existing controllers; add `[Authorize(Roles=…)]` where missing | 6 | **Done 2026-04-24** (commits `f41c410`, `7bda674`, `37013fc`, `c26d451`). `ProjectTemplatesController` refactored to inherit `CimsControllerBase`; two-tier role model retained (global `[Authorize(Roles)]` for admin-only endpoints, imperative `HasMinimumRole` for per-project gates). |
| T-S0-09 | CI pipeline: build + test on push (GitHub Actions), branch protection on `main` | 4 | User-driven portions where permissions require |
| T-S0-10 | Sprint close: retrospective, tag `v0.0-sprint-s0`, merge to `main` | 2 | |
| T-S0-11 | Invitation-token registration flow closing SR-S0-01 (anonymous cross-tenant user registration bypass) | 4 | **Done 2026-04-25** (commits `9b40a8d` entity + DbContext, `b528897` AddInvitations migration, `634705a` InvitationService, `3f95241` register flow, `3839468` bootstrap + invite endpoint). `CR-002`, `ADR-0011`. Breaking change: `RegisterRequest` no longer carries `OrganisationId`. |
| T-S0-12 | Lock project `AppointingPartyId` to caller's org with SuperAdmin bypass, closing SR-S0-02 | 1 | **Done 2026-04-24** (commit `c83a8a9`). `CR-002`, `ADR-0012`. |

**Total estimate:** 33h × 1.5 = **49h**
**Available sprint hours:** 6h × 15 working days × 80% = **72h**
Capacity OK. Remaining buffer absorbs investigation tasks and drift.

## This session (Session 3) covers

T-S0-01 (kickoff), T-S0-02 (ITenantContext), T-S0-03 (global query
filter). T-S0-04 (full runtime multi-tenant isolation integration
test) is **partially delivered**: a model-inspection test asserts the
filter is registered on each tenant-scoped entity, but the behavioural
test (two tenants, insert rows, query, verify isolation) needs an
in-memory EF provider. That package requires ADR + 24-hour wait per
PAFM-SD Ch 24.6. Deferred to a later S0 commit after ADR-0009 clears.

Remaining tasks land in later S0 sessions.

## Top 3 risks (PAFM-SD Appendix E)

1. **Applying the query filter may break existing queries.** Seed data,
   background migrations, and any cross-tenant admin code will fail
   once the filter lands. Mitigation: add filter to entities one at a
   time; full build + test after each.
2. **`ITenantContext` DI lifetime interacts awkwardly with `DbContext`.**
   Both are scoped; if the context is read during `OnModelCreating`
   (model build, singleton cache) it'll break. Mitigation: read
   `ITenantContext` at runtime in the filter expression via a captured
   `DbContext` field or a `EF.Property`-safe pattern.
3. **Existing controllers may be unit-tested against data that the new
   filter hides.** Tests that seeded rows without an OrganisationId or
   with a fake one will fail. Mitigation: seed test data with a
   consistent `OrganisationId` and wire the test `ITenantContext`
   accordingly.

## Daily entries

### Day 1 — 2026-04-24
Task today: T-S0-01 sprint kickoff.
Progress: Branch `sprint/s0-multi-tenant` created. Kickoff docs
(`docs/sprint-log/s0.md`, `docs/retrospectives/s0.md`,
`docs/current-sprint.md`) authored. DoD, tasks, risks captured.
Commits: `6816d6a` (chore(s0): sprint kickoff — DoD and tasks).
Blockers: none.
Decisions: Sessions 1-2 ISO 19650 validator work parked per PAFM-SD
Ch 0.4 — remains on `main` but will not be extended until S8. Candidate
ADR for next session: ADR-001 documenting this park decision.
*(Filed as `ADR-0008-park-iso19650-validator.md` after the
4-digit ADR-numbering convention firmed up; ADR-001 was the
in-progress working name in this Day 1 entry.)*
Drift risks noticed: Session 3 started as "add persistence to the
validator" — surfaced mid-session as Sprint 8 drift, corrected by
reading PAFM-SD. Counted as a near-miss; recovery was clean.
Tomorrow: T-S0-02 `ITenantContext`.
Wellbeing: N/A (session-based, not time-based).

### Session — 2026-04-24 (T-S0-08 role enforcement audit)
Task today: T-S0-08 role enforcement audit across controllers.
Progress: Audited every endpoint in `Controllers/Controllers.cs` and
`Controllers/ProjectTemplatesController.cs`. Adopted a two-tier role
model (global role via JWT claim `cims:role` for admin-only endpoints;
per-project role via `GetProjectRoleAsync` + `HasMinimumRole` for
project-scoped gates). Four commits landed:
- `f41c410` — `Program.cs` maps `cims:role` as `RoleClaimType` so
  `[Authorize(Roles=...)]` recognises the user's `GlobalRole`.
- `7bda674` — `ProjectTemplatesController` refactored to inherit
  `CimsControllerBase`; per-project membership enforced on list/read;
  `InformationManager+` on PUT content; `OrgAdmin/SuperAdmin` on POST
  provision.
- `37013fc` — `POST /api/v1/projects` now requires
  `OrgAdmin/SuperAdmin`.
- `c26d451` — `TaskTeamMember+` gate on five write endpoints:
  `POST documents`, `POST rfis`, `POST rfis/{id}/respond`,
  `POST actions`, `PATCH actions/{id}`.
Blockers: none. T-S0-04 and T-S0-06b still blocked behind ADR-0009's
24-hour wait on `Microsoft.EntityFrameworkCore.InMemory` 8.0.2;
scheduled remote agent for 2026-04-25 disabled this session (user
upgraded plan and continued locally instead).
Decisions: Spec literally asked for `[Authorize(Roles=…)]`; on audit
most role checks in CIMS are per-project, which attribute auth can't
express. Accepted the two-tier hybrid as the consistent approach —
records the decision here rather than opening a new ADR.
Drift risks noticed: None. Work stayed inside `Controllers/`, no touch
on `Services/Iso19650/` (parked per ADR-0008), no new packages.
Tomorrow: After 2026-04-25T12:42Z (ADR-0009 wait elapsed), land
T-S0-04 (multi-tenant isolation behavioural test) and T-S0-06b
(AuditInterceptor behavioural test).
Wellbeing: N/A (session-based).

### Session — 2026-04-25 (T-S0-11 invitation-token registration)
Task today: T-S0-11 closing SR-S0-01 (anonymous cross-tenant user
registration bypass) and a UI fix on the New Project button.

Progress:
- Five commits delivered T-S0-11 end-to-end:
  - `9b40a8d` — `Invitation` entity + DbSet + tenant query filter.
  - `b528897` — `AddInvitations` migration with corrected FK
    delete behaviours (NoAction on all three FKs to satisfy SQL
    Server's multi-cascade-path check; an earlier attempt
    `0fcff2a` failed on apply, was rolled back, regenerated).
  - `634705a` — `InvitationService` (`CreateAsync`,
    `ValidateAsync`, `MarkConsumedAsync`) with SHA-256 hashing
    of plaintext at rest and atomic single-use enforcement via
    `ExecuteUpdateAsync` on null-`ConsumedAt`.
  - `3f95241` — `RegisterRequest` drops `OrganisationId`, gains
    `InvitationToken`. `AuthService.RegisterAsync` now derives
    tenant from the validated invitation; bootstrap consumers
    auto-elevate to `GlobalRole = OrgAdmin`.
  - `3839468` — `POST /api/v1/organisations` mints a 24h
    bootstrap invitation in its response. New endpoint
    `POST /api/v1/organisations/{orgId}/invitations` lets
    OrgAdmin / SuperAdmin mint further invitations (caller's-org
    rule mirrors ADR-0012).
- `ADR-0011` written; role matrix, security review, change
  register, and this sprint log updated.

UI fix interleaved with T-S0-11:
- `9b6be43` + `045e0f5` — added `<Routes @rendermode="RenderMode.InteractiveServer" />`
  in `Components/App.razor` after diagnosing that the entire app
  was rendering as static SSR HTML and OnClick handlers never
  wired up. Initial attempt used the bare identifier
  `InteractiveServer`, which doesn't resolve without a `using
  static`; qualified to `RenderMode.InteractiveServer`.
- `12db136` — diagnostic instrumentation on `Projects.razor`
  (visible breadcrumb of `interactive` / `clicks` / `dialog`
  state, `ILogger<Projects>` trace lines) to confirm whether
  the click reached the server.
- `5c1435c` — replaced the inline `<MudDialog @bind-Visible>`
  pattern with an `@if (_showCreate)` + `<MudOverlay>` +
  `<MudPaper>` modal. The MudDialog inline form silently failed
  to render even when `_showCreate` flipped server-side; the
  render-tree-controlled overlay is guaranteed to appear when
  the bool is true. The diagnostic breadcrumb and verbose logs
  remain in place — flagged for removal in a later cleanup
  commit once T-S0-04 / T-S0-06b confirm everything else.

Blockers: none. T-S0-04 and T-S0-06b are now eligible
(ADR-0009 wait elapsed at 2026-04-25T12:42Z) but not yet
landed; queued for tomorrow.

Decisions:
- All three FKs on `Invitation` use `OnDelete(NoAction)` rather
  than the originally-intended `Cascade` + `SetNull` mix. SQL
  Server rejected the cycle. NoAction matches the dominant
  pattern in `CimsDbContext` (ProjectMember, RfiDocument,
  ActionItem) and is correct for an audit-style entity.
- Plaintext invitation tokens are returned exactly once and
  never persisted. Lost token = mint a new one. Recoverable
  tokens would require storing plaintext, which a DB-read
  compromise could enumerate.
- Email delivery deferred — out-of-band token sharing is
  acceptable until S13 Notifications module lands.

Drift risks noticed: None. All work stayed within S0 scope (F.1
DoD hardening). No touch on `Services/Iso19650/` (parked per
ADR-0008). No new packages.

Tomorrow: T-S0-04 multi-tenant isolation behavioural test and
T-S0-06b AuditInterceptor behavioural test (both unblock
2026-04-25T12:42Z). After those, T-S0-10 sprint close — tag
`v0.0-sprint-s0`, retrospective, merge to master.
Wellbeing: N/A (session-based).

### Session — 2026-04-25 (T-S0-04 + T-S0-06b behavioural tests)
Task today: land the two behavioural tests deferred behind the
ADR-0009 24-hour wait, which elapsed at 2026-04-25T12:42Z.
Followed the four-step plan baked into the (disabled) scheduled
remote agent prompt — package addition, isolation test, audit
test, close-out — but executed locally rather than re-enabling
the routine.

Progress:
- `0567adf` — added `Microsoft.EntityFrameworkCore.InMemory 8.0.2`
  to `CimsApp.Tests/CimsApp.Tests.csproj` per ADR-0009. Restore
  + solution build green.
- `92ef9ff` — `CimsApp.Tests/Data/CimsDbContextIsolationTests.cs`
  with four runtime scenarios proving the query filter shipped
  in T-S0-03 actually scopes reads. Added
  `CimsApp.Tests/TestDoubles/StubTenantContext.cs` — a settable
  `ITenantContext` reusable across the test suite.
- `98201e8` — `CimsApp.Tests/Services/Audit/AuditInterceptorBehaviourTests.cs`
  with five scenarios end-to-end through `SaveChangesAsync`:
  Insert / Update / Delete audit-row shape, AuditLog
  non-recursion, anonymous-tenant skip.
- This commit closes T-S0-04 and T-S0-06b in the sprint log.

Full-suite result at end of day: 46 passed / 0 failed.

Blockers: none. T-S0-10 (sprint close) is the only remaining S0
task. Diagnostic instrumentation added to `Projects.razor` during
the New-Project-button bug hunt (commit `12db136`) is still in
place — flagged for cleanup before sprint close.

Decisions:
- Tests do not re-enable the scheduled remote agent. The
  scheduled agent's prompt was used as a checklist for the
  manual implementation; both paths produce equivalent code, but
  running locally avoided the GitHub-App-not-connected setup
  warning and the round-trip for review.
- Hermetic test fixtures via `UseInMemoryDatabase(Guid.NewGuid().ToString())`,
  one DB per test, so tests run in parallel without cross-talk.

Drift risks noticed: None. Work stayed inside `CimsApp.Tests/`.

Tomorrow: T-S0-10 sprint close — clean up `Projects.razor`
diagnostic instrumentation, complete `docs/retrospectives/s0.md`,
tick the F.1 module-level DoD, tag `v0.0-sprint-s0`, merge to
master via PR.
Wellbeing: N/A (session-based).
