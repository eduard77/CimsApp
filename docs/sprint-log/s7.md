# Sprint S7 — Reporting & Dashboards

**Planned:** 2026-05-02 → 2026-05-23 (3 weeks)
**Roadmap reference:** PAFM-SD Ch 6.4, Phase 3
**Module DoD:** PAFM-SD Appendix F.8

## Sprint goal

Deliver per-role dashboards, the Monthly Project Report (MPR)
data aggregator, success-criteria KPI cards, and a basic custom
report builder. **First read-and-aggregate-shaped sprint** since
S0 — every prior module (S1..S6) was workflow-shaped.

The interesting questions for cadence-model evidence:
1. Does the existing `Core/<X>.cs` pure-function pattern extend
   to "report builder" arithmetic (MPR rollups, KPI computations)?
   Likely yes.
2. Does the same-session compression hold for read-heavy
   aggregation work, or does it slow down because every dashboard
   touches multiple modules' data shape simultaneously?
   Unknown — first read-shape sprint.

Inherits from S0..S6:
- Tenant query filter on every persisted entity (only one new
  entity in S7: `CustomReportDefinition`).
- Two-tier role model — read endpoints gated `membership`;
  custom-report-definition write paths `TaskTeamMember+`.
- Audit-twin pattern for the only mutating service
  (`CustomReportDefinitionsService`).
- v1.1-candidate decisions land inline (S5/S6-retro rule).
- State-machine implementation is now near-zero estimate (S6
  retro action — N/A for S7 since there are no new workflows).

## Branch / repo state

`sprint/s7-reporting-dashboards` is branched **off master**
(S0..S6 all merged 2026-05-02). PR opens directly to master at
sprint close.

## Module-level DoD (PAFM-SD Appendix F.8 — S7 Reporting & Dashboards)

Verbatim from PAFM-Software-Development v2.0 `.docx` Appendix F.8:

- [x] Role-specific dashboards (PM, CM, SM, IM, HSE, Client).
- [x] Monthly Project Report generation (PDF) per PAFM Ch 30.
      *PDF deferred to v1.1 / B-055 per kickoff Option A; v1.0
      ships the JSON data aggregator (T-S7-03).*
- [x] KPI cards tied to success criteria.
- [x] Basic custom report builder.

Inherited (must continue to hold):
- [x] Tenant isolation on every new entity.
- [x] Audit-twin atomicity per PR #33 (only relevant for
      `CustomReportDefinition` writes; dashboards / MPR / KPI are
      read-only).

## Sprint-level DoD (PAFM-SD Ch 10.2)

- [x] All committed tasks closed or explicitly deferred with reason.
- [x] All tests for implemented features pass. **808/808 green.**
- [ ] Sprint branch merged to master. *Pending PR.*
- [ ] Build green on CI. *Pending push + PR.*
- [x] Linter produces no new warnings (above S6 baseline).
- [x] README / ADR / code comments updated where relevant.
- [x] This Sprint Log complete through final day.
- [x] Retrospective (`docs/retrospectives/s7.md`) written.

## Decision required at kickoff: PDF rendering for the MPR

F.8 second bullet says: *"Monthly Project Report generation
(PDF) per PAFM Ch 30."* This requires a PDF library NuGet
package — a Ch 24.6 ADR-shaped decision with a 24-hour wait
between adding the package and exercising it.

**Three options:**

**Option A (recommended, default for this kickoff) — Defer
PDF rendering to v1.1.** Ship the MPR data aggregator as a
JSON DTO that the UI / future PDF rendering layer consumes.
Adds backlog entry **B-055 — MPR PDF generation per PAFM
Ch 30** carrying the library-choice decision (QuestPDF,
PdfSharpCore, HTML→PDF, etc) and the PAFM Ch 30 layout
detail (which is in the PDF spec, paste-on-request per the
`reference_pafm_spec.md` authorisation).
Trimmed F.8 fourth bullet still ticked: "MPR generation"
delivered as data aggregation; PDF render deferred. Honest
framing: the MPR data is the hard part; the PDF templating
is the easy part once the library is chosen.

**Option B — Add QuestPDF NuGet via ADR-0015 + 24h wait.**
ADR-0015 records the library choice + community-licence
applicability (Genera Systems is under-1M-revenue per the
QuestPDF Community License at time of writing — verify at
ADR-write time). 24-hour wait blocks T-S7-03 mid-sprint;
T-S7-04 / T-S7-05 can proceed in parallel during the wait.

**Option C — Ask Eduard to paste PAFM-CIMS-001 Ch 30** so we
know the canonical MPR structure before deciding library.
Pure-information option; doesn't move code.

Default: Option A. T-S7-03 ships a JSON-only MPR data
aggregator; PDF render → B-055.

## Task decomposition

Estimates in hours. Total multiplied by 1.5 per PAFM-SD Appendix E.

| ID | Task | Est (h) | Notes |
|---|---|---|---|
| T-S7-01 | Sprint kickoff — branch, F.8 DoD, tasks, capacity, PDF decision | 1 | This commit. |
| T-S7-02 | `DashboardsService` + 6 role-dashboards endpoints + DTOs + tests | 6 | Aggregation queries over S0..S6 entities. PM / CM / SM / IM / HSE / Client. HSE dashboard is sparse in v1.0 since the HSE module is S12. |
| T-S7-03 | MPR data aggregator + DTO + endpoint + tests | 4 | JSON-only DTO with sections per PAFM Ch 30 v1.0 best-effort layout (Executive Summary / Programme Status / Cost Status / Risk Status / Variations & Changes / Issues / Stakeholder Updates). **PDF rendering deferred to v1.1 / B-055** per Option A. |
| T-S7-04 | KPI cards endpoint mapping to PAFM-SD Ch 2.6 v1.0 success criteria | 3 | Module-usage counts (last 30 days), MPR currency, programme variance vs baseline (S4), CPI/SPI (S1 EVM), RFI response time, overdue-actions count. Single project-level endpoint returning the card list. |
| T-S7-05 | `CustomReportDefinition` entity + service + endpoints + tests | 5 | Saved-query model. Fields: Id, ProjectId, Name, EntityType (enum: Risk / Action / RFI / Variation / ChangeRequest / Tender / etc.), FilterJson, ColumnsJson, CreatedAt. v1.0 single-entity reports; cross-entity joins → v1.1 / B-056. |
| T-S7-06 | Tenant filter sweep on S7 entities | 1 | Add CustomReportDefinition to `TenantScoped[]` + behavioural filter test. Dashboards / MPR / KPI are read-only no new entities. |
| T-S7-07 | Sprint close — smoke walk → retro → tag → PR | 2 | Smoke walk runs as the FIRST close action per S5-retro rule. |

**Total estimate:** 22h × 1.5 = **33h**.

**Available sprint hours per kickoff template:**
6h × 15 working days × 80% = **72h**.

**Capacity headroom: 39h.** Most comfortable margin since S0;
fourth sprint in a row to fit cleanly without a CR (S3 / S5 / S6 /
S7). The headroom reflects (a) most F.8 work is read-shaped
aggregation rather than entity-domain modelling; (b) only one new
entity in this sprint; (c) the PDF deferral via Option A removes
the library-decision overhead.

## v1.1 candidates documented at kickoff (S5-retro discipline)

These will land as B-NNN entries in `docs/v1.1-backlog.md` in
the same commit as the task that surfaces them. Pre-flagged at
kickoff:

- **B-055 — MPR PDF generation per PAFM Ch 30.** The PDF
  rendering layer + library choice + PAFM Ch 30 canonical
  structure. Surfaced in T-S7-03 (Option A deferral).
- **B-056 — Custom report cross-entity joins.** v1.0 ships
  single-entity reports; multi-entity joins (e.g. "all Actions
  on overdue RFIs", "all Variations linked to Approved
  ChangeRequests") deferred. Surfaced in T-S7-05.
- **B-057 — Scheduled report runs + email distribution.**
  v1.0 reports run on demand only. Cron-shaped scheduled runs
  + email delivery deferred. Surfaced in T-S7-05.
- **B-058 — Custom report exports (CSV / Excel / PDF).** v1.0
  custom reports return JSON; export formats deferred. Surfaced
  in T-S7-05.
- **B-059 — HSE-specific dashboard cards.** S12 (Genera Systems
  QA, HS&E Integration) ships the HSE module; until then the
  HSE dashboard is sparse (project name + role membership).
  Surfaced in T-S7-02.

## Top 3 risks (PAFM-SD Appendix E)

1. **PAFM Ch 30 MPR layout is unknown at kickoff.** The PDF
   spec (PAFM-CIMS-001) Appendix is paste-on-request per
   `reference_pafm_spec.md`. T-S7-03 ships a v1.0 best-effort
   MPR structure inferred from PMBOK / NEC4 standard reporting
   templates. **Mitigation:** mark every MPR section with a
   `// PAFM Ch 30 reference: best-effort inference v1.0`
   comment so the v1.1 PDF-rendering work (B-055) can reconcile
   against the canonical Ch 30 paste at that time.
2. **Cross-module aggregation N+1 queries.** Each dashboard
   touches 4-8 modules' DbSets. Naive `await db.Foo.Where(...)
   .ToListAsync()` per module per request can produce
   >10 queries per dashboard load. **Mitigation:** v1.0 ships
   the simple per-module queries inline; if profiling shows
   real latency at pilot, B-NNN candidate is a single combined
   query per dashboard. Keep it simple first.
3. **`CustomReportDefinition` filter / columns persistence
   shape.** Storing `FilterJson` and `ColumnsJson` as serialised
   JSON in DB columns is the simple path; the structured-filter
   alternative (per-criterion table with type-safe operators)
   is over-engineered for v1.0. **Mitigation:** v1.0 stores
   JSON; queries deserialise + compose at run time. Migration
   to structured filters is a v1.1 candidate if a real
   customer hits the typed-query / index-friendly need.

Note (per S6 retro action): the cadence-breaker prediction
section is dropped from this kickoff — seven sprints of
falsification is enough. Substantive code: 1 session expected
unless something genuinely changes.

## Daily entries

### Day 1 — 2026-05-02 (kickoff)
Task today: T-S7-01 sprint kickoff.
Progress: Branch `sprint/s7-reporting-dashboards` created off
master (`cf65456` post-S6 merge). PAFM-SD `.docx` Appendix F.8
extracted directly via the standing
`reference_pafm_spec.md` authorisation; four bullets verbatim.
Pre-cut estimate 22h × 1.5 = 33h vs the 72h formal capacity —
**39h headroom**, the largest cushion since S0. **No CR needed.**
Commits: this commit (kickoff scaffold + F.8 backfill).
Blockers: none.
Decisions:
- Branch off master (clean, post-S6 merge).
- **Option A on PDF rendering**: defer to v1.1 / B-055; T-S7-03
  ships JSON-only MPR data aggregator. Avoids the Ch 24.6
  24-hour wait + library-choice ADR-0015 mid-sprint.
- v1.0 custom report builder is single-entity only; cross-entity
  joins → B-056. v1.0 returns JSON; export formats → B-058.
  Scheduled runs + email delivery → B-057.
- HSE dashboard is sparse in v1.0 — the HSE module itself is
  S12 (Genera Systems QA, HS&E Integration). HSE-specific cards
  → B-059.
- Drop the cadence-breaker prediction section (S6 retro action).
Drift risks noticed:
- Top-3 risk #1: PAFM Ch 30 layout unknown — best-effort
  inference; reconcile at B-055 time.
- Top-3 risk #2: dashboard N+1 queries — v1.0 simple, profile
  later.
- Top-3 risk #3: custom-report filter persistence — JSON in v1.0;
  structured-filter table is v1.1.
Tomorrow: T-S7-02 `DashboardsService` + 6 role-dashboards
endpoints + DTOs + tests.
Wellbeing: N/A (session-based).

### Day 2 — 2026-05-02 (substantive code + close)

Same calendar day as kickoff — single-session compression
holds for the **eighth consecutive sprint** (S0..S7). Per
the S6-retro discipline change, cadence is now reported as
a single line: substantive code: 1 session.

T-S7-02 DashboardsService + 6 role dashboards:
- `fe28a0f` feat: DashboardCardType + DashboardCardDto +
  DashboardDto records; DashboardsService class with 6 role
  methods (PM / CM / SM / IM / HSE / Client); 6 endpoints
  under `/api/v1/projects/{projectId}/dashboards/{role}`;
  role-matrix.md updated; HSE sparse v1.0 placeholder per
  kickoff (B-059 inline)
- `9b1979f` 9 behavioural tests (PM / CM / SM / IM / HSE /
  Client + cross-tenant 404 + EndDate fallback + no-WWP
  placeholder)

T-S7-03 MPR data aggregator:
- `689b25c` feat: MprDto with seven sections (ExecutiveSummary
  / Programme / Cost / Risk / Changes / Issues / Stakeholders);
  ReportingService.GenerateMonthlyProjectReportAsync; default
  period = last full calendar month UTC; optional from/to
  query params; risk severity buckets per PMI 5x5 (high≥15,
  medium 7-12, low ≤6); membership-only GET endpoint at
  `/api/v1/projects/{projectId}/reports/mpr`; **B-055 v1.1
  backlog inline** per S5-retro discipline (PDF rendering +
  PAFM Ch 30 layout reconciliation)
- `32060ce` 9 behavioural tests covering all seven sections
  + period window + cross-tenant 404

T-S7-04 KPI cards:
- `4fd2dc4` feat: KpiCardsDto + ReportingService.
  GetProjectKpiCardsAsync; 7 cards mapped to Ch 2.6 v1.0
  success criteria — Module Activity (last 30d), MPR Period
  Coverage, Critical Path Activities, Cost Spent vs Budget
  (CPI proxy), Schedule Completion (SPI proxy), RFI Avg
  Response (last 30d), Overdue Actions; honest v1.0 proxies
  with subtitles calling out what's a proxy
- `14a2c8a` 7 behavioural tests (default placeholders +
  module activity 30d window + cost spent + schedule
  completion + RFI avg + overdue + cross-tenant 404)

T-S7-05 CustomReportDefinition + CRUD + Run:
- `c71e0ca` feat: CustomReportEntityType enum (Risk /
  ActionItem / Rfi / Variation / ChangeRequest); entity +
  DTOs; CustomReportDefinitionsService (List / Get / Create
  / Update / Delete / Run); Core/CustomReportRunner.cs with
  per-entity field allow-list + JSON-equality interpreter +
  reflection match/project; 6 endpoints under
  `/api/v1/projects/{projectId}/custom-reports`; migration
  AddCustomReportDefinitions with (ProjectId, Name) unique
  index filtered IsActive = 1 for soft-delete + name reuse;
  **B-056..B-060 v1.1 backlog inline** (cross-entity joins,
  scheduled runs, exports, HSE-specific dashboard cards,
  rich filter operators)
- `dd40c51` 11 behavioural tests covering CRUD + Run + name
  uniqueness + filter / column validation + cross-tenant 404

T-S7-06 tenant filter sweep:
- `4199f30` CustomReportDefinition added to
  CimsDbContextTenantFilterTests.TenantScoped[]; new
  ReportingFilterTests.cs with two-tenant cross-isolation
  test mirroring ProcurementFilterTests pattern. Dashboards
  / MPR / KPI introduced no new entities.

T-S7-07 sprint close (this commit + tag + PR):

**Test count delta:** 771 (post-S6) → **808** (close of S7).
**+37 tests this sprint** — smallest delta since S0 (+47).
Reflects the **read-shape vs workflow-shape** distinction:
S6 needed full state-machine fixture coverage (149 new
tests); S7 needed surface-area + behavioural tests but no
state machines, no atomic-spawn integration, no per-row
audit-shape tests. **First sprint to fall below the +60
test floor** of S1..S6 — not a quality drop, a domain
shape change.

**Blockers:** none.

**Decisions made mid-sprint:**
- **Option A on PDF rendering** (decided at kickoff):
  defer PDF generation to v1.1 / B-055; T-S7-03 ships
  JSON-only MPR data aggregator. Avoids ADR-0015 24-hour
  wait + library-choice mid-sprint.
- HSE dashboard explicit sparse v1.0 — single placeholder
  card pointing at S12 (HSE module integration). B-059 in
  v1.1 backlog inline.
- KPI cards CPI / SPI as **honest proxies** — actuals/budget
  ratio and completed-activities/total ratio rather than
  fake-EVM. Subtitles surface the proxy nature; genuine EVM
  needs per-line earned-value progress signal that is a
  v1.1 / S8+ concern.
- Custom reports v1.0 = **single-entity + pure equality**;
  per-entity field allow-list captured in
  `Core/CustomReportRunner.cs`. Cross-entity joins → B-056;
  rich operators → B-060; scheduled runs → B-057; exports
  → B-058.
- In-memory filter pass for Run (load project rows, filter
  by reflection). Pushdown to SQL is bounded by single-
  project volume; v1.1 perf concern only if pilot data
  exceeds the simple-query envelope.

**Drift risks engaged:**
- Top-3 risk #1 (PAFM Ch 30 layout unknown) — sidestepped
  via Option A. MPR DTO comment marks the layout as
  "best-effort inference v1.0"; B-055 will reconcile.
- Top-3 risk #2 (dashboard N+1 queries) — v1.0 ships
  simple per-module queries inline as planned. Not
  profiled at pilot data volumes; "consolidate per-
  dashboard query" is a v1.1 candidate if real latency
  surfaces.
- Top-3 risk #3 (custom-report filter persistence shape)
  — JSON in DB columns chosen as planned; structured-
  filter table is v1.1 if a real customer hits the
  typed-query / index-friendly need. Pure-equality v1.0
  keeps the JSON contract simple.

**Smoke walk** (run as the FIRST close action per the
S5-retro re-shaped rule) — mental walk-through against
the 808-test surface area:

| Surface | Coverage |
|---|---|
| 6 role dashboards (PM/CM/SM/IM/HSE/Client) | 9 DashboardsServiceTests + cross-tenant 404 |
| MPR JSON aggregator (7 sections) | 9 ReportingServiceTests covering each section + period window + fallback + 404 |
| KPI cards (7 cards mapped to Ch 2.6) | 7 KpiCardsServiceTests (defaults + each non-trivial card + 404) |
| Custom report CRUD + Run + validation | 11 CustomReportDefinitionsServiceTests covering full lifecycle |
| Tenant isolation on the new entity | 1 model-level (CimsDbContextTenantFilterTests adds CustomReportDefinition) + 1 behavioural (ReportingFilterTests cross-tenant) |
| Audit-twin pattern on Custom Report writes | Covered implicitly: AuditService wired in service constructor; Create / Update / Delete each emit `custom_report.{action}` events under the same SaveChangesAsync as the entity write |
| Per-entity field allow-list rejection | Covered: `Creates_rejects_unknown_filter_field`, `Creates_rejects_unknown_column` |
| HSE dashboard sparse placeholder | Covered: `GetHseDashboardAsync_returns_sparse_S12_placeholder` |
| Estimated finish max(EarlyFinish) fallback to Project.EndDate | Covered (Dashboards + MPR test) |
| Variations period-window filter on CreatedAt + DecidedAt | Covered: `Variations_period_window_filters_correctly_by_CreatedAt_and_DecidedAt` |

All 808 tests green. No production bug surfaced during the
walk-through. Read-shape sprint produces a smaller test
delta but the shape was always going to be that way.

PR + tag: pending user confirmation per "shared-state
action" guidance. Locally on `sprint/s7-reporting-
dashboards` with 12 commits ahead of master.

Wellbeing: N/A (session-based).
