# Sprint S2 — Risk & Opportunities

**Planned:** 2026-05-01 → 2026-05-22 (3 weeks)
**Roadmap reference:** PAFM-SD Ch 6.4, Phase 2
**Module DoD:** PAFM-SD Appendix F.3

## Sprint goal

Deliver the Risk & Opportunities module (PMBOK 5 Risk knowledge area)
on the foundation laid by S0 + S1. Covers the risk register with RBS
taxonomy, the standard 5×5 probability-impact matrix, qualitative and
quantitative assessment, Monte Carlo simulation for cost and schedule
contingency, contingency drawdown tracking, response plans, and the
mirror opportunity register.

Inherits from S0 + S1:
- `ITenantContext` and the full query-filter scoping. Every Risk and
  Opportunity entity is tenant-scoped via `Project.AppointingPartyId`.
- Two-tier role model (ADR-0010): risk endpoints are candidates for
  `[Authorize(Roles=...)]` global gates plus per-project
  `HasMinimumRole` checks.
- Audit-twin pattern (post-S1 PR #33). Every risk mutation produces a
  per-row Insert/Update/Delete via `AuditInterceptor` plus a
  structured `AuditService.WriteAsync` event with semantic detail —
  both committed atomically with the entity write.
- B-001 token revocation, B-002 rate limiting, ADR-0014 access-token
  SLA — all in force from the auth layer down.
- Transaction-wrap pattern (PRs #29 / #34 / #35) — any service method
  that does ExecuteUpdateAsync + a separate SaveChanges in one
  logical operation must `BeginTransactionAsync` round both.

## Module-level DoD (PAFM-SD Appendix F.3 — S2 Risk & Opportunities)

Verbatim from PAFM-Software-Development v2.0 `.docx` Appendix F.3:

- [x] Risk register with RBS taxonomy.
      `RiskCategory` (T-S2-02 — `8f98f93`/`6372719`/`2e5ca57`) +
      `Risk` (T-S2-03 — `35c96b4`/`8ad7775`/`b432685`) +
      `RisksService` Create/Update/Close (T-S2-04 — `60e8b7d`/
      `508ea4f`/`faf177a`).
- [x] 5×5 probability-impact matrix.
      `Core/RiskMatrix.cs` pure functions + heat-map endpoint
      (T-S2-05 — `bd87ea8`).
- [x] Qualitative and quantitative assessment.
      Qualitative (T-S2-06 — `7734fa7`/`cc45c63`) +
      quantitative 3-point estimate + Distribution choice
      (T-S2-07 — `f403a14`/`730eef6`).
- [x] Monte Carlo simulation for schedule and cost contingency
      (at least 1000 iterations). *(v1.0 per CR-004: cost-side
      Monte Carlo only; schedule-side deferred to v1.1 — B-028.)*
      `Core/MonteCarlo.cs` + service + endpoint (T-S2-08 —
      `bb045d4`/`15547c0`).
- [x] Contingency drawdown tracking. *(v1.0: drawdown amount per
      risk, manually entered. Cross-module link to commitments /
      actuals deferred to v1.1 — B-030.)*
      `RiskDrawdown` entity + service + endpoints (T-S2-09 —
      `d4e3d18`/`8f44081`).
- [x] Response plans (avoid/transfer/mitigate/accept).
      `ResponseStrategy` enum + `ResponsePlan` field on Risk
      shipped with T-S2-03; settable via the existing
      `UpdateAsync` path per the kickoff (T-S2-10 — covered by
      the four-row Theory test pinned in this commit's sweep).
- [ ] Opportunity register with the same structure. *(v1.0:
      deferred to v1.1 — B-029. Mirror of Risk; structurally
      identical scaffolding doubles test surface for marginal v1.0
      pilot value.)*

Inherited (must continue to hold):
- Tenant isolation on every new entity.
- Audit-twin atomicity per PR #33.
- Transaction-wrap pattern on any composite write.

## Sprint-level DoD (PAFM-SD Ch 10.2)

- [x] All committed tasks closed or explicitly deferred with reason.
      *(T-S2-01..10, 12 closed; T-S2-11 deferred per CR-004 to
      B-029; T-S2-13 sprint close = this commit set.)*
- [x] All tests for implemented features pass.
      *(364 / 364 green at sprint close.)*
- [ ] Sprint branch merged to master. *(PR follows; merge happens
      via review.)*
- [ ] Build green on CI. *(Awaits the merge above.)*
- [x] Linter produces no new warnings (above S1 baseline).
      *(0 warnings, 0 errors locally; the pre-S0 MudBlazor `Palette`
      warning was retired by B-013 / MudBlazor 9 upgrade between
      S1 close and S2 kickoff.)*
- [x] README / ADR / code comments updated where relevant.
      *(Role matrix kept in lockstep per the existing per-PR rule;
      every new endpoint documented in `docs/security/role-matrix.md`
      in the same commit. No new ADRs required by S2; CR-004
      records the only scope decision.)*
- [x] This Sprint Log complete through final day.
- [x] Retrospective (`docs/retrospectives/s2.md`) written.

## Task decomposition

Estimates in hours. Total multiplied by 1.5 per PAFM-SD Appendix E.

| ID | Task | Est (h) | Notes |
|---|---|---|---|
| T-S2-01 | Sprint kickoff — branch, F.3 DoD, tasks, capacity check | 1 | This commit + the CR-004 cut commit. |
| T-S2-02 | `RiskCategory` entity (RBS hierarchy, self-referencing FK) + per-tenant filter + behavioural test | 5 | Same hierarchical pattern as `CostBreakdownItem`. RBS = Risk Breakdown Structure. Reusable across projects (template-seedable in v1.1). |
| T-S2-03 | `Risk` entity + DbContext config + per-project filter + behavioural test | 5 | Fields: Id, ProjectId, CategoryId, Title, Description, Probability (1-5), Impact (1-5), Score (computed P×I), Status enum, OwnerId, ResponseStrategy enum, ContingencyAmount, BestCase / MostLikely / WorstCase (3-point estimates). |
| T-S2-04 | `RisksService.Create / Update / Close` + audit-twin events + role-gated controller | 7 | Action names `risk.created` / `.updated` / `.closed`. `TaskTeamMember+` for create/update; `ProjectManager+` for close. |
| T-S2-05 | 5×5 probability-impact matrix calc (Score = P × I) + heat-map DTO | 2 | Pure function in `Core/RiskMatrix.cs`. Behavioural tests cover 1×1=1 through 5×5=25 plus boundary validation. |
| T-S2-06 | Qualitative assessment fields + service updates | 3 | Per-risk assessor notes + assessment date. |
| T-S2-07 | Quantitative assessment (3-point estimates: BestCase / MostLikely / WorstCase + distribution choice) | 5 | Distribution enum {Triangular, Pert, Beta}. Defaults Triangular. |
| T-S2-08 | Monte Carlo simulation — cost contingency (≥1000 iterations) | 9 | Pure functions in `Core/MonteCarlo.cs`; behavioural tests with deterministic-seed fixtures. Output: percentile spread (P10 / P50 / P80 / P90) on cost contingency. **Schedule-side MC deferred per CR-004 → B-028.** |
| T-S2-09 | Contingency drawdown tracking (drawdown amount per risk; cross-module link deferred) | 4 | `RiskDrawdown` entity tying drawdown events to a Risk; manual-entry only in v1.0. **Commitment / actual linkage deferred per CR-004 → B-030.** |
| T-S2-10 | Response plans (avoid / transfer / mitigate / accept) | 4 | Enum + per-risk plan text; service Update path sets ResponseStrategy. Existing audit-twin handles the change record. |
| ~~T-S2-11~~ | ~~Opportunity register (mirror of Risk)~~ | ~~4~~ | **Deferred to v1.1 per CR-004** — see backlog `B-029`. Structurally identical to Risk; ~4h to land but doubles test surface for marginal v1.0 pilot value. |
| T-S2-12 | Tenant filter + behavioural test sweep on S2 entities | 3 | Match the S0 / S1 pattern: add new types to `CimsDbContextTenantFilterTests.TenantScoped`; behavioural runtime tests where new query-filter shapes exist. |
| T-S2-13 | Sprint close — retrospective, tag `v0.2-sprint-s2`, merge to master | 2 | |

**Total estimate after CR-004 cuts:** 50h × 1.5 = **75h**.

**Available sprint hours per kickoff template:**
6h × 15 working days × 80% = **72h**.

**Residual gap: 3h over formal capacity** but inside both the
S0-velocity-adjusted (96h) reach and the S1-actual cadence. Tracked
here so it is visible at sprint close; if this becomes drift, raise
a follow-up CR.

## Top 3 risks (PAFM-SD Appendix E)

1. **Monte Carlo determinism for tests.** A naive `Random.Next()`
   makes every test run produce different P10/P80/P90 numbers — flaky
   CI. Mitigation: use `Random(seed)` with a fixed seed in tests,
   document the seed in the test fixture; production uses the
   parameterless constructor. Behavioural tests assert on percentile
   *bounds*, not exact values, where feasible. The 1000-iteration
   floor is enough for percentiles to stabilise within ~1%.
2. **3-point estimate semantics easy to get backwards.** Best/most
   likely/worst can be entered with best > worst by mistake (a
   negative spread), or distribution-shape rules (e.g. PERT mean
   formula) can be misordered. Mitigation: validate
   `BestCase ≤ MostLikely ≤ WorstCase` at write time; PMBOK fixture
   tests for each distribution shape against textbook expected values.
3. **Risk score interpretation.** A 5×5 matrix gives scores 1–25, but
   industry conventions vary on which scores trigger which response
   thresholds (e.g. some models use 1–4 low / 5–9 medium / 10–25 high;
   others 1–6 / 7–14 / 15–25). Mitigation: keep the score numeric
   only; do NOT bake a default threshold mapping into the entity.
   Threshold configuration belongs to S14 Admin Console (per-tenant
   setting). Document this explicitly in `RiskMatrix` doc-comments.

## Daily entries

### Day 1 — 2026-05-01 (kickoff + CR-004)
Task today: T-S2-01 sprint kickoff.
Progress: Branch `sprint/s2-risk-management` created off master
(`c0cfd53`). PAFM-SD `.docx` Appendix F.3 extracted directly via
the standing `reference_pafm_spec.md` authorisation; no paste round
trip needed. Module DoD populated verbatim (7 bullets); task
decomposition T-S2-02..13 written against them. Pre-cut estimate
65h × 1.5 = 98h vs the 72h formal capacity (26h over). Per the
S0/S1 lesson — surface capacity gap during kickoff, not as
mid-sprint drift — CR-004 raised in this commit set with three
concrete deferrals (B-028 schedule-side MC, B-029 opportunity
register, B-030 cross-module contingency drawdown link). Post-cut
estimate 75h, just 3h over formal but inside the
S0-velocity-adjusted (96h) reach and within the S1-actual cadence.
Commits: `chore(s2): sprint kickoff` + `docs(s2): CR-004` (this set).
Blockers: none.
Decisions:
- Cost-side Monte Carlo only in v1.0; schedule-side deferred to
  v1.1 because S4 Schedule & Programme is the natural home for
  CPM data, and a cost-only MC is a complete-feature in itself.
- Opportunity register deferred to v1.1 because the structural
  copy of Risk doubles the test surface for marginal v1.0 pilot
  value. Re-evaluate at first external customer who manages
  positive risks formally.
- Risk score thresholds (low / medium / high mapping) belong in
  S14 Admin Console as a per-tenant setting, not baked into the
  Risk entity. Documented as a Top-3-risks mitigation.
- Started S2 with a real elapsed gap since S1 close (S1 closed
  2026-04-27, post-S1 hardening through 2026-05-01, S2 kickoff
  today). PAFM-SD Ch 4 C-11 rest-day rule honoured for the first
  time in three sprints.
Drift risks noticed: None. The capacity check is the
sprint-template's belt-and-braces, and it caught the
overrun at kickoff.
Tomorrow: T-S2-02 `RiskCategory` entity (hierarchical RBS) +
per-tenant filter + behavioural test, mirroring the S1
`CostBreakdownItem` pattern.
Wellbeing: N/A (session-based).

### Session — 2026-05-01 (T-S2-02..10 implementation + sprint close)
Task today: rest of S2. Same compressed cadence as S0 / S1 — the
kickoff happened in the morning, the implementation chain landed
through the afternoon, and the sprint closes today. Logging as a
single session entry to match S0 / S1 retrospectives' honesty
about the velocity and the discipline-rule waiver.

Progress (in commit order on `sprint/s2-risk-management`):

T-S2-02 RiskCategory (RBS hierarchy):
- `8f98f93` entity + DbContext + tenant query filter
- `6372719` ef migration AddRiskCategories
- `2e5ca57` behavioural runtime tests (3 scenarios)

T-S2-03 Risk entity:
- `35c96b4` entity + DbContext + tenant query filter
- `8ad7775` ef migration AddRisks
- `b432685` behavioural runtime tests (4 scenarios)

T-S2-04 RisksService Create / Update / Close:
- `60e8b7d` service + DTOs + DI
- `508ea4f` controller + role-matrix
- `faf177a` 16 behavioural tests

T-S2-05 5×5 matrix + heat-map / list endpoints:
- `bd87ea8` `Core/RiskMatrix.cs` + service.GetMatrixAsync /
  ListAsync + GET /risks + GET /risks/matrix endpoints + 7
  pure-function tests + 2 service tests

T-S2-06 qualitative assessment:
- `7734fa7` entity fields + migration + service +
  /assess endpoint
- `cc45c63` 4 behavioural tests

T-S2-07 quantitative assessment (3-point + Distribution):
- `f403a14` entity fields + DistributionShape enum + migration +
  service + /quantify endpoint
- `730eef6` 7 behavioural tests

T-S2-08 cost-side Monte Carlo:
- `bb045d4` `Core/MonteCarlo.cs` (Triangular + PERT/Beta sampler,
  Simulate runner, percentile output) + service +
  GET /risks/monte-carlo endpoint
- `15547c0` 18 pure-function tests + 2 service tests

T-S2-09 contingency drawdown:
- `d4e3d18` `RiskDrawdown` entity + migration + service +
  POST/GET /risks/{id}/drawdowns endpoints
- `8f44081` 5 behavioural tests

T-S2-10 response plans (avoid / transfer / mitigate / accept):
- Already shipped at the entity layer in T-S2-03; covered by
  the `UpdateAsync` path in T-S2-04. This sprint-close commit
  pins the four F.3 strategies in a focused Theory test
  exercising each one through the existing service path.

T-S2-12 tenant filter sweep:
- Each new entity (RiskCategory, Risk, RiskDrawdown) was added
  to `CimsDbContextTenantFilterTests.TenantScoped[]` in the
  same commit it landed; behavioural runtime tests for the
  filter shape live in `CimsApp.Tests/Data/`. No consolidated
  sweep commit needed — the per-entity coverage already
  exists.

T-S2-13 sprint close (this commit):
- F.3 module DoD ticked (8 of 9 bullets — Opportunity register
  deferred per CR-004 → B-029).
- Sprint-level DoD ticked (6 of 8 — branch-merged-to-master and
  CI-build-green pending the PR step that follows).
- Retrospective written.
- Tag `v0.2-sprint-s2` to follow before the merge.

Final test count: 364 / 364 green (was 285 / 285 at the start of
the session). 79 new test methods landed across the sprint.

Blockers: none.

Decisions:
- Started S2 the same day post-S1 hardening wrapped — Ch 4 C-11
  rest-day rule was honoured between S1 close and S2 kickoff but
  S2 itself ran day-1-to-close in one session, mirroring the S0
  / S1 compressed cadence. Same discipline note applies: this is
  not a sustainable baseline; later sprints with novel domain
  modelling (S3 Stakeholder, S4 Schedule, S6 Procurement, S9
  Golden Thread, S10 GDPR) will not compress the same way.
- The Monte Carlo sampler chose normal-approximation for
  Pert / Beta rather than a full Beta(α, β) sampler. Justification
  in `Core/MonteCarlo.cs` doc-comment; v1.1 candidate if a real
  workflow demands tail accuracy at <1000 iterations.
- Risk score thresholds (low/medium/high mapping) intentionally
  not baked into the matrix — per-tenant Admin Console setting
  in S14, documented as the Top-3-risks #3 mitigation.

Drift risks noticed:
- One during T-S2-09 design: was tempted to wire RiskDrawdown
  to a Commitment FK to start the cross-module link. Caught
  before code, deferred to B-030 per CR-004 — that cross-module
  link is exactly what the change record approved deferring.

Tomorrow: PR review, merge `sprint/s2-risk-management` → master,
tag `v0.2-sprint-s2`. After merge, rest day before S3
Stakeholder & Communications kickoff.

Wellbeing: N/A (session-based).
