# CIMS v1.1 backlog

Per PAFM-SD commitment C-03: "I will keep a v1.1 backlog so
deferred work is visible and cannot be 'lost'." Items here are
out of scope for v1.0 but formally acknowledged as future work.

This is not a to-do list. Items move into a future sprint only
after v1.0 ships and a sprint kickoff has considered them against
the Chapter 17 change control process.

## How to use this file

- **Add an item** when a feature is consciously deferred — either
  out-of-scope per PAFM Ch 3, or identified as hardening work that
  exceeds the current sprint's budget.
- **Every item** has: source (what discovered it), category,
  short description, and why it is deferred rather than closed now.
- **Removing an item** happens when it lands in a sprint. Link the
  removal to the implementing commit in the commit message.
- Items are not prioritised here; prioritisation happens at v1.1
  planning time using the current PAFM and updated success criteria.

## Entries

### B-001 — Access-token lifetime policy + role-change revocation — PARTIAL CLOSE 2026-04-28

**Source:** Security review SR-S0-04 (2026-04-24).
**Category:** Auth hardening.
**Description.** JWT access tokens carry the `cims:role` claim baked
in at issue time. Demoting an admin leaves them with residual
authority until the token expires. Define `AccessMinutes` as a
documented SLA in an ADR and add a revocation path that forces token
refresh on role change.

**Status (2026-04-28, partial close).** Revocation primitive
landed on branch `feature/b-001-token-revocation`:

- `User.TokenInvalidationCutoff` (datetime, nullable) column +
  `AddUserTokenInvalidationCutoff` migration.
- `CimsApp.Services.Auth.TokenRevocation.IsRevoked(user,
  tokenIssuedAtUtc)` pure function returning true when the
  bearer should be rejected — covers (a) null / inactive user
  (closes the today-bug where deactivating a user did not
  invalidate their existing token) and (b) `iat < cutoff`.
- `JwtBearerEvents.OnTokenValidated` hook in `Program.cs`
  loads the User after signature/issuer/audience/lifetime
  pass and runs the helper. Uses `IgnoreQueryFilters` since
  the User lookup runs pre-tenant-context.
- `AuthService.RevokeUserTokensAsync(userId)` bumps cutoff to
  `UtcNow`. No controller endpoint yet — caller is the future
  role-change / deactivation / password-reset path.
- 8 tests cover the revocation rules and the service method
  (null user, inactive user, no-cutoff, cutoff-after-iat,
  cutoff-before-iat, equality boundary, plus
  RevokeUserTokensAsync happy path and unknown-user 404).

**Remaining v1.1.**
- ADR documenting `AccessMinutes` as a security SLA — the
  60-minute residual-authority window between revoke and
  refresh is now bounded but not formally captured.
- Public endpoint(s) that call `RevokeUserTokensAsync` —
  natural call sites are a future role-demote endpoint, a
  user-deactivate endpoint, and self-service "log out
  everywhere".

### B-002 — Rate limiting and abuse protection on anonymous endpoints — PARTIAL CLOSE 2026-04-28

**Source:** Security review SR-S0-06.
**Category:** Platform hardening.
**Description.** `POST /auth/register`, `POST /auth/login`,
`POST /auth/refresh`, and `POST /organisations` have no rate limit,
CAPTCHA, or email verification. Add ASP.NET Core rate-limiting
middleware, progressive back-off on login failures, CAPTCHA or
email-verification on organisation creation.

**Status (2026-04-28, partial close).** Rate-limiting half landed
on branch `feature/b-002-rate-limiting` via ASP.NET Core's built-in
`Microsoft.AspNetCore.RateLimiting`. Two named policies:
- `anon-login`: 5 req/min per IP — applied to `/auth/login` only
  (credential-testing target).
- `anon-default`: 10 req/min per IP — applied to `/auth/register`,
  `/auth/refresh`, and anonymous `POST /organisations`.

Fixed-window partitioning by `RemoteIpAddress`. 429 returned on
limit hit. Manual smoke verified: 5 sequential `POST /auth/login`
return application errors (expected — no users seeded), the 6th
returns 429 from the limiter before the endpoint executes.

**Remaining v1.1.** CAPTCHA on organisation creation, progressive
back-off on login (e.g. 1s → 5s → 30s), and email verification.
Each is meaningful pre-customer hardening but each warrants its
own scope; rate limiting is the lowest-risk and highest-value
piece, shipped here.

### B-003 — Constant-time register path

**Source:** Security review SR-S0-07.
**Category:** Auth hardening.
**Description.** `AuthService.RegisterAsync` first checks email
existence, then organisation existence. Differing latencies leak a
small email-enumeration oracle. Merge the checks into a single
constant-time flow, or defer to an auth-hardening sprint with other
timing-oracle audits.
**Why deferred.** Low impact — emails are generally non-secret.
Batch with other auth hardening.

### B-004 — Contractual-relationship check at project creation

**Source:** Security review SR-S0-08.
**Category:** Billing / admin.
**Description.** Beyond the tenant-ownership check (SR-S0-02, which
is S0-blocking), create-project has no validation that the calling
organisation is entitled to create projects — contract active,
billing up to date, seat count not exceeded, plan supports the
feature. Belongs to the admin console / billing work.
**Why deferred.** S14 Admin Console is the natural home; premature
here.

### ~~B-005 — Assignee ownership check on `PATCH actions/{id}`~~ — CLOSED 2026-04-28

**Source:** `docs/security/role-matrix.md#known-deferred-checks`.
**Category:** Domain authorization.
**Closed.** Service-level guard added in `ActionsService.UpdateAsync`:
caller must be the assignee OR `ProjectManager+`. Unassigned actions
(`AssigneeId == null`) remain open to any `TaskTeamMember+` so the
gap-of-ownership case doesn't grind. Four behavioural tests in
`OwnershipCheckTests` cover assignee-can-update, non-assignee-forbidden,
PM-override, and unassigned-fallback.

### ~~B-006 — Responder verification on `POST rfis/{id}/respond`~~ — CLOSED 2026-04-28

**Source:** `docs/security/role-matrix.md#known-deferred-checks`.
**Category:** Domain authorization.
**Closed.** Service-level guard added in `RfiService.RespondAsync`:
when `AssignedToId` is set, caller must be that user OR an
`InformationManager+`. Unassigned RFIs (`AssignedToId == null`)
remain open to any `TaskTeamMember+`. IM is the natural escalation
when an assigned responder is unavailable. Four parallel
behavioural tests in `OwnershipCheckTests` cover responder-can-respond,
non-responder-forbidden, IM-override, and unassigned-fallback.

### B-007 — Admin scoping on `GET /api/v1/organisations`

**Source:** `docs/security/role-matrix.md#known-deferred-checks`.
**Category:** Admin UI.
**Description.** Endpoint returns all organisations visible through
the tenant query filter. For ordinary users this is their own org;
for `SuperAdmin` this is all orgs. Acceptable by ADR-0003 but likely
evolves when the admin console (S14) is built.
**Why deferred.** Correct today per ADR-0003. Revisit at S14.

### B-008 — Reconcile `Services/Iso19650/Iso19650ReferenceData.cs`
with `Core/Iso19650Codes.cs`

**Source:** CR-001 impact note; memory
`project_roadmap_position.md`.
**Category:** Technical debt from Sessions 1–2.
**Description.** Two divergent reference-data sources for ISO
19650 codes exist. Must be unified at S8 kickoff when the parked
validator is integrated with the naming wizard / MIDP / TIDP flow.
**Why deferred.** S8 is the natural home. Parked per ADR-0008.

### B-009 — SSO / federated IdP support and multi-role-claim-type
handling

**Source:** Security review SR-S0-03 (forward-looking).
**Category:** Auth architecture.
**Description.** `RoleClaimType = "cims:role"` currently maps a
single custom claim to ASP.NET's role concept. If federated SSO is
added, an external IdP may emit `ClaimTypes.Role`. Revisit whether
both claim types should be honoured or whether a claims-transformer
maps external roles to `cims:role`.
**Why deferred.** No SSO customer in v1.0. Revisit before first B2B
customer onboards.

### B-010 — Mobile-native application

**Source:** PAFM-SD Ch 3.3.
**Category:** Platform.
**Description.** Native iOS/Android app. Out of v1.0 by PAFM-SD Ch 3.3.
**Why deferred.** Anti-scope. Only in scope if market feedback post
v1.0 demands it; wrap-as-PWA may be enough.

### B-011 — Multi-language / multi-region

**Source:** PAFM-SD Ch 3.7.
**Category:** Platform.
**Description.** CIMS v1.0 is English-only, GBP, UK-regs. Localisation
moves to v1.1+.
**Why deferred.** Anti-scope.

### ~~B-013 — MudBlazor 7 upgrade~~ — CLOSED 2026-04-27

**Source:** S0 retrospective notes-for-next-sprint; CR-003 (S1
scope cut).
**Category:** Tech debt / UI framework upgrade.
**Description.** Upgrade MudBlazor 6.20.0 → 7.x. Resolves the
pre-existing `Palette` deprecation warning at
`Components/Layout/MainLayout.razor:82` and removes future risk
on inline-dialog patterns (the silent-no-op bug the S0
retrospective flagged on `<MudDialog @bind-Visible>`).
**Closed.** Promoted to a small post-S1 task and landed on
branch `chore/mudblazor-7-upgrade` immediately after the S1
PR merged. Went straight to MudBlazor **9.4.0** (latest stable;
supports net8.0) rather than stepping through 7.x → 8.x → 9.x.
Breaking changes addressed: `Palette` → `PaletteLight`
(MudBlazor 7), `Typography.Default = new Default { ... }` →
`new DefaultTypography { ... }` (v7),
`MudAutocomplete.SearchFunc` gained a `CancellationToken`
parameter (v8), `MudDatePicker.FullWidth` removed (v7+).
130 / 130 tests still green; dev server smoked clean
(`/health` 200, `/` 200). No new linter warnings; the
pre-existing `Palette` deprecation warning is gone.

### B-014 — Construction Act payment notice and pay-less notice flows

**Source:** PAFM-SD `.docx` Appendix F.2; CR-003 deferred from S1.
**Category:** S1 fast-follow OR v1.1.
**Description.** Statutory notice flows under the
Housing Grants, Construction and Regeneration Act 1996 — payment
notice issued by paying party, pay-less notice for any reduction.
Statutory deadlines (5 days from due date for payment notice,
7 days before final date for pay-less). PDF generation via the
existing template path used by payment certificates.
**Why deferred.** Depends on T-S1-09 payment certificate
generation landing first; S1 capacity (72h formal, 96h
S0-adjusted) does not fit both with confidence. Bring back as a
fast-follow mini-sprint after S1 close OR roll into S5 Change
Control where the workflow conventions overlap. Compliance
risk: shipping payment certs without these flows means UK
construction users would have to track notices off-system —
acceptable for v1.0 internal pilot, not for an external customer.
Re-evaluate before any external onboarding.

### B-015 — Final account schedule

**Source:** PAFM-SD `.docx` Appendix F.2; CR-003 deferred from S1.
**Category:** Late-stage feature (v1.0 fast-follow OR v1.1).
**Description.** Project-closeout rollup view tying CBS lines,
final variations valuations, retention release, and any pending
commitments into a single agreed-final-account document.
**Why deferred.** Late-stage in the project lifecycle — most
projects do not need it until practical completion. None of the
S0 success-criteria pilot projects are at PC yet. Defer until
the pilot reaches that stage OR until the same pilot demands it.

### ~~B-017 — Per-CBS-line schedule + progress signal~~ — CLOSED 2026-04-27

**Source:** S1 retrospective (`docs/retrospectives/s1.md`).
Consolidates three service-integration deferrals taken
independently across S1.

**Closed 2026-04-27.** All three downstream service-integration
wire-ups landed on branch `feature/b-017-line-schedule-progress`
immediately after the MudBlazor 9 upgrade. PR #4.

Primitive shipped:
- `ScheduledStart`, `ScheduledEnd`, `PercentComplete decimal(5,4)
  ∈ [0, 1]` columns on `CostBreakdownItem`.
- `SetLineScheduleAsync` and `SetLineProgressAsync` service
  methods plus two PM+ endpoints
  (`PUT /cbs/{itemId}/schedule`, `PUT /cbs/{itemId}/progress`).
- Audit-twin actions `cbs.line_schedule_set` and
  `cbs.line_progress_set`.

Three downstream wire-ups shipped:
- **T-S1-07 EVM service integration.**
  `CostService.GetEvmSnapshotAsync(projectId, dataDate)` walks
  every CBS line summing BAC = Σ Budget, EV = Σ Budget ×
  PercentComplete, PV = Σ Budget × scheduleFraction(dataDate,
  ScheduledStart, ScheduledEnd). AC = Σ ActualCost. Feeds
  `Evm.Calculate` from T-S1-07 unchanged. New endpoint
  `GET /evm?dataDate=...`.
- **T-S1-09 payment-cert valuation auto-derive.**
  `PaymentCertificateDto` gains a computed
  `DerivedValuationFromProgress = Σ Budget × PercentComplete`
  field. Stored `CumulativeValuation` remains the assessor-stated
  source of truth per ADR-0013; the derived field is the
  progress-based guide alongside it.
- **T-S1-11 per-CBS-line cashflow breakdown.**
  `CostService.GetCashflowByLineAsync` returns one
  `CashflowLineSeries` per CBS line, each with one
  `CashflowLinePeriodPoint` per CostPeriod. BaselinePlanned per
  point = Budget × overlap-fraction(period range, schedule
  range). Actual per point = Σ ActualCost where line + period
  match. New endpoint `GET /cashflow/by-line`.

146 / 146 suite green at sprint-close commit on the branch.
**Category:** Cost-domain data primitive (v1.1).
**Description.** v1.0 has no per-CBS-line scheduled-spend curve
and no per-CBS-line progress (percent-complete) signal. The
absence blocks three otherwise-shippable service integrations:

- **T-S1-07 EVM service integration.** `CimsApp/Core/Evm.cs`
  ships the pure math (PV/EV/AC → CPI/SPI/EAC/TCPI). A
  service that reads project data and produces an
  `EvmSnapshot` needs PV (planned value at data date — needs
  the schedule baseline) and EV (earned value — needs
  progress).
- **T-S1-09 payment-certificate valuation auto-derivation.**
  `PaymentCertificate.CumulativeValuation` is currently
  manually entered by the assessor. With per-line progress,
  it could be auto-derived as `Σ (CBS line Budget × percent
  complete)` per ADR-0013 NEC4 PWDD.
- **T-S1-11 per-CBS-line cashflow breakdown.**
  `CostService.GetCashflowAsync` returns project-level
  totals only. Per-line cashflow needs each line's planned
  spend distributed across periods, which is the same
  schedule-baseline data the EVM PV needs.

A minimum useful schema would add:

- `CostBreakdownItem.ScheduledStart` / `ScheduledEnd` (date
  range — when work on this line is planned).
- `CostBreakdownItem.PercentComplete` (0..100, or stored as
  decimal 0..1).
- A method (linear by default) to distribute a line's Budget
  across its scheduled date range, intersected with project
  CostPeriods, to produce the planned-cashflow contribution
  per (line, period).

All three integrations then become read-only computations on
top of this primitive. No service rewrites are needed; just
new methods that consume the new fields.

**Why deferred.** Adding the primitive would have inflated the
S1 scope by another ~8-12h (entity columns, migration,
service updates across three sites, behavioural tests) on top
of the CR-003-trimmed 96h budget that already sits 24h over
formal capacity. Each downstream integration was deferred
locally with a forward-pointing comment; this item rolls them
up so the v1.1 planner sees the cross-cutting work as one
decision rather than three.

### B-016 — Variations workflow: full 6-state machine

**Source:** PAFM-SD `.docx` Appendix F.2; CR-003 trimmed for S1.
**Category:** Process polish (v1.1).
**Description.** Restore the full F.2 variations workflow:
raise → **assess** → approve(delegation) → **instruct** →
**value** → **agree** (intermediate states bolded). T-S1-08 in
S1 ships only the core raise → approve → reject states; this
backlog item adds the four intermediate states.
**Why deferred.** The intermediate states are mostly
process-tracking on top of the core state machine. Approve /
reject is the load-bearing decision; assess / instruct / value /
agree are auditable positions in the workflow but do not change
the financial outcome. Acceptable to defer for v1.0 pilot use.

### B-012 — AI-assisted features in the product itself

**Source:** PAFM-SD Ch 3.4.
**Category:** Product.
**Description.** Embedding LLMs/AI features inside CIMS (document
summarisation, draft responses, anomaly detection) is v1.1+.
**Why deferred.** Anti-scope. AI-assisted *development* is fine and
in use; AI-assisted *product* is v1.1.

## Source legend

- `SR-S0-XX` — Sprint 0 security review finding
  (`docs/security/s0-review-2026-04-24.md`).
- `CR-XXX` — Change register entry (`docs/change-register.md`).
- Role matrix — `docs/security/role-matrix.md`.
- PAFM-SD Ch X.Y — software development discipline manual,
  `Notes/PAFM_Software_Development_v2.0.docx`.
