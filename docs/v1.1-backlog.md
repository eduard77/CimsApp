# CIMS v1.1 backlog

Per PAFM-SD commitment C-03: "I will keep a v1.1 backlog so
deferred work is visible and cannot be 'lost'." Items here are
out of scope for v1.0 but formally acknowledged as future work.

This is not a to-do list. Items move into a future sprint only
after v1.0 ships and a sprint kickoff has considered them against
the Chapter 17 change control process.

## How to use this file

- **Add an item** when a feature is consciously deferred — either
  out-of-scope per PAFM Ch 3, or identified as hardening work that
  exceeds the current sprint's budget.
- **Every item** has: source (what discovered it), category,
  short description, and why it is deferred rather than closed now.
- **Removing an item** happens when it lands in a sprint. Link the
  removal to the implementing commit in the commit message.
- Items are not prioritised here; prioritisation happens at v1.1
  planning time using the current PAFM and updated success criteria.

## Entries

### ~~B-001 — Access-token lifetime policy + role-change revocation~~ — CLOSED 2026-04-28

**Source:** Security review SR-S0-04 (2026-04-24).
**Category:** Auth hardening.
**Description.** JWT access tokens carry the `cims:role` claim baked
in at issue time. Demoting an admin leaves them with residual
authority until the token expires. Define `AccessMinutes` as a
documented SLA in an ADR and add a revocation path that forces token
refresh on role change.

**Status (2026-04-28, partial close).** Revocation primitive
landed on branch `feature/b-001-token-revocation`:

- `User.TokenInvalidationCutoff` (datetime, nullable) column +
  `AddUserTokenInvalidationCutoff` migration.
- `CimsApp.Services.Auth.TokenRevocation.IsRevoked(user,
  tokenIssuedAtUtc)` pure function returning true when the
  bearer should be rejected — covers (a) null / inactive user
  (closes the today-bug where deactivating a user did not
  invalidate their existing token) and (b) `iat < cutoff`.
- `JwtBearerEvents.OnTokenValidated` hook in `Program.cs`
  loads the User after signature/issuer/audience/lifetime
  pass and runs the helper. Uses `IgnoreQueryFilters` since
  the User lookup runs pre-tenant-context.
- `AuthService.RevokeUserTokensAsync(userId)` bumps cutoff to
  `UtcNow`. No controller endpoint yet — caller is the future
  role-change / deactivation / password-reset path.
- 8 tests cover the revocation rules and the service method
  (null user, inactive user, no-cutoff, cutoff-after-iat,
  cutoff-before-iat, equality boundary, plus
  RevokeUserTokensAsync happy path and unknown-user 404).

**Closed 2026-04-28.** Both remaining items shipped on branch
`feature/b-001-close-revoke-endpoints`:

- **ADR-0014** — `docs/adr/ADR-0014-access-token-residual-authority-sla.md`
  documents `AccessMinutes` as a security parameter (default 60,
  range 5–60), enumerates the security-sensitive User mutations
  required to call the revocation primitive, and clarifies the
  IsActive permanent-reject vs cutoff-window semantic split.
- **Public endpoints** consuming the primitive:
  - `POST /api/v1/auth/logout-everywhere` (any authenticated
    user, self-service) → `RevokeOwnTokensAsync(actorId)`.
  - `POST /api/v1/users/{userId}/revoke-tokens`
    (OrgAdmin / SuperAdmin) → `RevokeUserTokensAsync(userId,
    tenant)`. OrgAdmin tenant-scoped; SuperAdmin ignores filter.
  - `POST /api/v1/users/{userId}/deactivate`
    (OrgAdmin / SuperAdmin) → `DeactivateUserAsync(userId,
    tenant)`. Sets IsActive = false AND bumps cutoff.
  Cross-tenant attempts 404 (existence not leaked).

7 new tests (174 / 174 suite green): self-service happy + 404,
OrgAdmin same-org / cross-org / SuperAdmin paths for revoke,
deactivate IsActive + cutoff happy + cross-tenant 404 with
no-mutation assertion.

### ~~B-002 — Rate limiting and abuse protection on anonymous endpoints~~ — CLOSED 2026-04-28

**Source:** Security review SR-S0-06.
**Category:** Platform hardening.
**Description.** `POST /auth/register`, `POST /auth/login`,
`POST /auth/refresh`, and `POST /organisations` have no rate limit,
CAPTCHA, or email verification. Add ASP.NET Core rate-limiting
middleware, progressive back-off on login failures, CAPTCHA or
email-verification on organisation creation.

**Status (2026-04-28, partial close).** Rate-limiting half landed
on branch `feature/b-002-rate-limiting` via ASP.NET Core's built-in
`Microsoft.AspNetCore.RateLimiting`. Two named policies:
- `anon-login`: 5 req/min per IP — applied to `/auth/login` only
  (credential-testing target).
- `anon-default`: 10 req/min per IP — applied to `/auth/register`,
  `/auth/refresh`, and anonymous `POST /organisations`.

Fixed-window partitioning by `RemoteIpAddress`. 429 returned on
limit hit. Manual smoke verified: 5 sequential `POST /auth/login`
return application errors (expected — no users seeded), the 6th
returns 429 from the limiter before the endpoint executes.

**Closed 2026-04-28.** Progressive back-off shipped on branch
`feature/b-002-progressive-backoff-and-b-003-obsolete`:

- `CimsApp.Services.Auth.LoginAttemptTracker` (singleton, backed
  by `IMemoryCache`). Per-IP failed-attempt counter with a
  15-minute sliding window. Lockout threshold 5.
- `AuthService.LoginAsync` short-circuits with `AppException(429,
  "LOGIN_LOCKOUT")` BEFORE any DB lookup when the caller's IP is
  locked out — credential-stuffing campaigns burn through their
  rate-limit budget without learning anything new.
- `try/catch (AppException ex when ex.StatusCode == 401)` wraps
  the auth flow: only 401-class credential failures count toward
  lockout (validation errors do not — operator typos shouldn't
  tighten the screw). Successful login resets the counter.

11 new tests across two files (185 / 185 suite green):
`LoginAttemptTrackerTests` covers the tracker rules in isolation;
`LoginProgressiveBackOffTests` covers the AuthService integration
with both stub-and-real-tracker paths, including a five-failure
end-to-end that confirms even valid credentials are rejected
once locked.

CAPTCHA on organisation creation and email verification remain
out of scope for v1.0 — neither is required by the internal
pilot. Each warrants its own backlog item if the pre-customer
posture demands them; promotion happens at v1.1 planning.

### ~~B-003 — Constant-time register path~~ — OBSOLETE 2026-04-28

**Source:** Security review SR-S0-07.
**Category:** Auth hardening.
**Description.** `AuthService.RegisterAsync` first checks email
existence, then organisation existence. Differing latencies leak a
small email-enumeration oracle.

**Resolution.** The oracle described here was eliminated as a
side effect of the **T-S0-11 invitation-token rewrite** (closes
SR-S0-01, ADR-0011). The post-T-S0-11 RegisterAsync flow is:

  1. ValidateAsync(invitationToken, email) — single
     `Invitations.FirstOrDefaultAsync(i => i.TokenHash == hash)`
     followed by in-memory checks (consumed, expired, email
     bind). One DB hit regardless of which check fails.
  2. If invitation valid: check email duplicate; find org;
     persist user.

A caller without a valid invitation token NEVER reaches the
email duplicate check or the organisation lookup — there is no
timing differential between "valid email" and "invalid email"
because the duplicate check is gated behind a successful
invitation validation. The email-enumeration vector this item
described is closed by the gate, not by constant-time
arithmetic.

No code change required to retire this item. Filed obsolete
rather than closed because the work envisaged was never the
work that landed.

### B-004 — Contractual-relationship check at project creation

**Source:** Security review SR-S0-08.
**Category:** Billing / admin.
**Description.** Beyond the tenant-ownership check (SR-S0-02, which
is S0-blocking), create-project has no validation that the calling
organisation is entitled to create projects — contract active,
billing up to date, seat count not exceeded, plan supports the
feature. Belongs to the admin console / billing work.
**Why deferred.** S14 Admin Console is the natural home; premature
here.

### ~~B-005 — Assignee ownership check on `PATCH actions/{id}`~~ — CLOSED 2026-04-28

**Source:** `docs/security/role-matrix.md#known-deferred-checks`.
**Category:** Domain authorization.
**Closed.** Service-level guard added in `ActionsService.UpdateAsync`:
caller must be the assignee OR `ProjectManager+`. Unassigned actions
(`AssigneeId == null`) remain open to any `TaskTeamMember+` so the
gap-of-ownership case doesn't grind. Four behavioural tests in
`OwnershipCheckTests` cover assignee-can-update, non-assignee-forbidden,
PM-override, and unassigned-fallback.

### ~~B-006 — Responder verification on `POST rfis/{id}/respond`~~ — CLOSED 2026-04-28

**Source:** `docs/security/role-matrix.md#known-deferred-checks`.
**Category:** Domain authorization.
**Closed.** Service-level guard added in `RfiService.RespondAsync`:
when `AssignedToId` is set, caller must be that user OR an
`InformationManager+`. Unassigned RFIs (`AssignedToId == null`)
remain open to any `TaskTeamMember+`. IM is the natural escalation
when an assigned responder is unavailable. Four parallel
behavioural tests in `OwnershipCheckTests` cover responder-can-respond,
non-responder-forbidden, IM-override, and unassigned-fallback.

### ~~B-007 — Admin scoping on `GET /api/v1/organisations`~~ — CLOSED 2026-04-28

**Source:** `docs/security/role-matrix.md#known-deferred-checks`.
**Category:** Admin UI / org-enumeration hardening.
**Closed.** Re-reading the role-matrix during the post-S1 audit
pass found that the original "Acceptable by ADR-0003" framing
was based on a wrong premise: the role-matrix entry claimed the
tenant query filter scoped the list, but `Organisation` is
intentionally unfiltered per ADR-0003 (it's the tenant anchor
used during pre-auth sign-up / login). The endpoint was
exposing every active organisation's `Name` and `Code` to any
authenticated caller — small leak but a real one.

Fix landed at the controller layer (Organisation can't get a
DbContext-level filter without breaking pre-auth flows):
non-SuperAdmin callers now see only their own organisation;
SuperAdmin retains the wider view per ADR-0007. Five
behavioural tests in `OrganisationsListScopingTests` cover
OrgAdmin / TaskTeamMember / no-GlobalRole / SuperAdmin /
inactive-org-excluded paths.

The "evolves when admin console is built" note in the original
deferral is moot — the new behaviour is the right default and
S14's admin console can layer "see appointed orgs through
project membership" on top of it as a separate concern.

### B-008 — Reconcile `Services/Iso19650/Iso19650ReferenceData.cs`
with `Core/Iso19650Codes.cs`

**Source:** CR-001 impact note; memory
`project_roadmap_position.md`.
**Category:** Technical debt from Sessions 1–2.
**Description.** Two divergent reference-data sources for ISO
19650 codes exist. Must be unified at S8 kickoff when the parked
validator is integrated with the naming wizard / MIDP / TIDP flow.
**Why deferred.** S8 is the natural home. Parked per ADR-0008.

### B-009 — SSO / federated IdP support and multi-role-claim-type
handling

**Source:** Security review SR-S0-03 (forward-looking).
**Category:** Auth architecture.
**Description.** `RoleClaimType = "cims:role"` currently maps a
single custom claim to ASP.NET's role concept. If federated SSO is
added, an external IdP may emit `ClaimTypes.Role`. Revisit whether
both claim types should be honoured or whether a claims-transformer
maps external roles to `cims:role`.
**Why deferred.** No SSO customer in v1.0. Revisit before first B2B
customer onboards.

### B-010 — Mobile-native application

**Source:** PAFM-SD Ch 3.3.
**Category:** Platform.
**Description.** Native iOS/Android app. Out of v1.0 by PAFM-SD Ch 3.3.
**Why deferred.** Anti-scope. Only in scope if market feedback post
v1.0 demands it; wrap-as-PWA may be enough.

### B-011 — Multi-language / multi-region

**Source:** PAFM-SD Ch 3.7.
**Category:** Platform.
**Description.** CIMS v1.0 is English-only, GBP, UK-regs. Localisation
moves to v1.1+.
**Why deferred.** Anti-scope.

### ~~B-013 — MudBlazor 7 upgrade~~ — CLOSED 2026-04-27

**Source:** S0 retrospective notes-for-next-sprint; CR-003 (S1
scope cut).
**Category:** Tech debt / UI framework upgrade.
**Description.** Upgrade MudBlazor 6.20.0 → 7.x. Resolves the
pre-existing `Palette` deprecation warning at
`Components/Layout/MainLayout.razor:82` and removes future risk
on inline-dialog patterns (the silent-no-op bug the S0
retrospective flagged on `<MudDialog @bind-Visible>`).
**Closed.** Promoted to a small post-S1 task and landed on
branch `chore/mudblazor-7-upgrade` immediately after the S1
PR merged. Went straight to MudBlazor **9.4.0** (latest stable;
supports net8.0) rather than stepping through 7.x → 8.x → 9.x.
Breaking changes addressed: `Palette` → `PaletteLight`
(MudBlazor 7), `Typography.Default = new Default { ... }` →
`new DefaultTypography { ... }` (v7),
`MudAutocomplete.SearchFunc` gained a `CancellationToken`
parameter (v8), `MudDatePicker.FullWidth` removed (v7+).
130 / 130 tests still green; dev server smoked clean
(`/health` 200, `/` 200). No new linter warnings; the
pre-existing `Palette` deprecation warning is gone.

### B-014 — Construction Act payment notice and pay-less notice flows

**Source:** PAFM-SD `.docx` Appendix F.2; CR-003 deferred from S1.
**Category:** S1 fast-follow OR v1.1.
**Description.** Statutory notice flows under the
Housing Grants, Construction and Regeneration Act 1996 — payment
notice issued by paying party, pay-less notice for any reduction.
Statutory deadlines (5 days from due date for payment notice,
7 days before final date for pay-less). PDF generation via the
existing template path used by payment certificates.
**Why deferred.** Depends on T-S1-09 payment certificate
generation landing first; S1 capacity (72h formal, 96h
S0-adjusted) does not fit both with confidence. Bring back as a
fast-follow mini-sprint after S1 close OR roll into S5 Change
Control where the workflow conventions overlap. Compliance
risk: shipping payment certs without these flows means UK
construction users would have to track notices off-system —
acceptable for v1.0 internal pilot, not for an external customer.
Re-evaluate before any external onboarding.

### B-015 — Final account schedule

**Source:** PAFM-SD `.docx` Appendix F.2; CR-003 deferred from S1.
**Category:** Late-stage feature (v1.0 fast-follow OR v1.1).
**Description.** Project-closeout rollup view tying CBS lines,
final variations valuations, retention release, and any pending
commitments into a single agreed-final-account document.
**Why deferred.** Late-stage in the project lifecycle — most
projects do not need it until practical completion. None of the
S0 success-criteria pilot projects are at PC yet. Defer until
the pilot reaches that stage OR until the same pilot demands it.

### ~~B-017 — Per-CBS-line schedule + progress signal~~ — CLOSED 2026-04-27

**Source:** S1 retrospective (`docs/retrospectives/s1.md`).
Consolidates three service-integration deferrals taken
independently across S1.

**Closed 2026-04-27.** All three downstream service-integration
wire-ups landed on branch `feature/b-017-line-schedule-progress`
immediately after the MudBlazor 9 upgrade. PR #4.

Primitive shipped:
- `ScheduledStart`, `ScheduledEnd`, `PercentComplete decimal(5,4)
  ∈ [0, 1]` columns on `CostBreakdownItem`.
- `SetLineScheduleAsync` and `SetLineProgressAsync` service
  methods plus two PM+ endpoints
  (`PUT /cbs/{itemId}/schedule`, `PUT /cbs/{itemId}/progress`).
- Audit-twin actions `cbs.line_schedule_set` and
  `cbs.line_progress_set`.

Three downstream wire-ups shipped:
- **T-S1-07 EVM service integration.**
  `CostService.GetEvmSnapshotAsync(projectId, dataDate)` walks
  every CBS line summing BAC = Σ Budget, EV = Σ Budget ×
  PercentComplete, PV = Σ Budget × scheduleFraction(dataDate,
  ScheduledStart, ScheduledEnd). AC = Σ ActualCost. Feeds
  `Evm.Calculate` from T-S1-07 unchanged. New endpoint
  `GET /evm?dataDate=...`.
- **T-S1-09 payment-cert valuation auto-derive.**
  `PaymentCertificateDto` gains a computed
  `DerivedValuationFromProgress = Σ Budget × PercentComplete`
  field. Stored `CumulativeValuation` remains the assessor-stated
  source of truth per ADR-0013; the derived field is the
  progress-based guide alongside it.
- **T-S1-11 per-CBS-line cashflow breakdown.**
  `CostService.GetCashflowByLineAsync` returns one
  `CashflowLineSeries` per CBS line, each with one
  `CashflowLinePeriodPoint` per CostPeriod. BaselinePlanned per
  point = Budget × overlap-fraction(period range, schedule
  range). Actual per point = Σ ActualCost where line + period
  match. New endpoint `GET /cashflow/by-line`.

146 / 146 suite green at sprint-close commit on the branch.
**Category:** Cost-domain data primitive (v1.1).
**Description.** v1.0 has no per-CBS-line scheduled-spend curve
and no per-CBS-line progress (percent-complete) signal. The
absence blocks three otherwise-shippable service integrations:

- **T-S1-07 EVM service integration.** `CimsApp/Core/Evm.cs`
  ships the pure math (PV/EV/AC → CPI/SPI/EAC/TCPI). A
  service that reads project data and produces an
  `EvmSnapshot` needs PV (planned value at data date — needs
  the schedule baseline) and EV (earned value — needs
  progress).
- **T-S1-09 payment-certificate valuation auto-derivation.**
  `PaymentCertificate.CumulativeValuation` is currently
  manually entered by the assessor. With per-line progress,
  it could be auto-derived as `Σ (CBS line Budget × percent
  complete)` per ADR-0013 NEC4 PWDD.
- **T-S1-11 per-CBS-line cashflow breakdown.**
  `CostService.GetCashflowAsync` returns project-level
  totals only. Per-line cashflow needs each line's planned
  spend distributed across periods, which is the same
  schedule-baseline data the EVM PV needs.

A minimum useful schema would add:

- `CostBreakdownItem.ScheduledStart` / `ScheduledEnd` (date
  range — when work on this line is planned).
- `CostBreakdownItem.PercentComplete` (0..100, or stored as
  decimal 0..1).
- A method (linear by default) to distribute a line's Budget
  across its scheduled date range, intersected with project
  CostPeriods, to produce the planned-cashflow contribution
  per (line, period).

All three integrations then become read-only computations on
top of this primitive. No service rewrites are needed; just
new methods that consume the new fields.

**Why deferred.** Adding the primitive would have inflated the
S1 scope by another ~8-12h (entity columns, migration,
service updates across three sites, behavioural tests) on top
of the CR-003-trimmed 96h budget that already sits 24h over
formal capacity. Each downstream integration was deferred
locally with a forward-pointing comment; this item rolls them
up so the v1.1 planner sees the cross-cutting work as one
decision rather than three.

### B-016 — Variations workflow: full 6-state machine

**Source:** PAFM-SD `.docx` Appendix F.2; CR-003 trimmed for S1.
**Category:** Process polish (v1.1).
**Description.** Restore the full F.2 variations workflow:
raise → **assess** → approve(delegation) → **instruct** →
**value** → **agree** (intermediate states bolded). T-S1-08 in
S1 ships only the core raise → approve → reject states; this
backlog item adds the four intermediate states.
**Why deferred.** The intermediate states are mostly
process-tracking on top of the core state machine. Approve /
reject is the load-bearing decision; assess / instruct / value /
agree are auditable positions in the workflow but do not change
the financial outcome. Acceptable to defer for v1.0 pilot use.

### ~~B-023 — `AddMemberAsync` org-match check (SR-S0-05)~~ — CLOSED 2026-04-28

**Source:** Sprint 0 security review SR-S0-05.
**Category:** Domain authorization.
**Background.** SR-S0-05 (rated Low at the S0 review) noted that
`ProjectsService.AddMemberAsync` did not verify the new member's
`User.OrganisationId` against the project's `AppointingPartyId`.
A PM in Org A could add a User from Org B as a project member;
the row would persist but the user's tenant filter (Project
filtered by AppointingPartyId == tenant.OrganisationId) would
hide the project from them, leaving an orphan ProjectMember row.
The finding was filed in the S0 review doc but never promoted
to the v1.1 backlog.

**Closed.** Service-layer guard added in `AddMemberAsync`:

- Look up the project (`NotFoundException` if missing).
- Look up the user (`IgnoreQueryFilters` since this is an admin
  add path, `NotFoundException` if missing).
- If `user.OrganisationId != project.AppointingPartyId`:
  `ValidationException(["The user must belong to the project's
  appointing organisation"])`.
- Existing idempotent re-add (Role + IsActive update) preserved.

5 behavioural tests in `Services/Projects/AddMemberOrgMatchTests.cs`
cover same-org happy path, cross-org rejected with no-row-written
guarantee, unknown user, unknown project, and the idempotent
re-add (existing inactive membership reactivated with new role,
no duplicate row).

**Future expansion (post-v1.0).** When B2B contractor membership
via `ProjectAppointment` lands, the check widens to "member's
org is the AppointingParty OR is an appointed contractor on
this project". For v1.0 the strict same-org rule is correct
per ADR-0012.

### B-018 — `LoginAttemptTracker` is single-instance only

**Source:** B-002 close (2026-04-28). Self-flagged at the
landing commit.
**Category:** Platform hardening / horizontal-scale readiness.
**Description.** The progressive back-off counter
(`CimsApp.Services.Auth.LoginAttemptTracker`) is backed by
`IMemoryCache` registered as a process-singleton in `Program.cs`.
That works for the v1.0 deployment shape (single web process)
but does not survive horizontal scale-out: each replica would
have its own counter, so an attacker could spread credential
testing across replicas and avoid the lockout entirely.

**Why deferred.** v1.0 deploys as a single instance; the
limitation has zero exposure today. When (and if) the
deployment scales horizontally, the natural fixes are (a)
move the counter into a shared distributed cache (Redis,
SQL Server in-memory OLTP) or (b) move the counter onto the
DB as an `Authn` table with row-level locking. (a) is faster
and cheaper; (b) gives free durability across restarts.

Pre-customer hardening: revisit before any deployment
topology that includes more than one `CimsApp` web process.

### ~~B-019 — Refresh-token bulk-revoke alongside cutoff bump~~ — CLOSED 2026-04-28

**Source:** ADR-0014 §3 self-flag during the B-001 close
(2026-04-28). Surfaced same day during the B-001 close-up
review.
**Category:** Auth hardening.
**Description.** ADR-0014 §3 originally deferred refresh-token
sweep on the reasoning "access tokens are short-lived". That
reasoning was incomplete — refresh tokens produce fresh access
tokens whose `iat` is greater than the cutoff, defeating "log
out everywhere" for multi-device users until the refresh token
naturally expires (`RefreshExpiresDays`, default 7).

**Closed.** `AuthService` gains a private
`SweepActiveRefreshTokensAsync(userId)` helper that sets
`RevokedAt = UtcNow` on every active (non-revoked, non-expired)
refresh token for the user. Called from all three revoke entry
points: `RevokeOwnTokensAsync`, `RevokeUserTokensAsync(admin)`,
`DeactivateUserAsync`. ADR-0014 §3 amended to match (see the
Amendment log on the ADR). Four behavioural tests in
`Services/Auth/RefreshTokenSweepTests.cs` covering: active
revoked, already-revoked unchanged, expired untouched, other
users' tokens not affected (collateral-damage guard).

189 / 189 suite green at the closing commit.

### ~~B-021 — Structured `AuditService` events on auth-domain mutations~~ — CLOSED 2026-04-28

**Source:** Post-S1 auth-mutation audit
(`docs/security/post-s1-auth-mutation-audit-2026-04-28.md`,
finding AM-04).
**Category:** Audit-log discoverability.
**Closed.** `AuthService` ctor gains `AuditService` (5th
parameter). The three revoke entry points emit structured
events with the refresh-token sweep count folded into the
parent action's detail (rather than a separate
`auth.refresh_tokens_swept` event):

- `RevokeOwnTokensAsync` → `auth.user_self_revoke` with
  `{ refreshTokensSwept }`. Actor = self.
- `RevokeUserTokensAsync` (admin) → `auth.user_admin_revoke`
  with `{ targetUserId, refreshTokensSwept }`. Actor = admin
  (read from `tenant.UserId`).
- `DeactivateUserAsync` → `auth.user_deactivated` with
  `{ targetUserId, refreshTokensSwept }`. Actor = admin.

The audit-twin pattern from S1 is now consistently applied:
per-row audit on User + RefreshToken rows (via interceptor)
PLUS the structured action-name event (via AuditService).
3 new behavioural tests in `AuthServiceInputValidationTests`
verify each emission, including the refreshTokensSwept count
and the actor / target distinction.

192 / 192 suite green at the closing commit.

### B-024 — Optimistic concurrency control on mutable entities

**Source:** Post-S1 secondary-mutation audit
(`docs/security/post-s1-secondary-mutation-audit-2026-04-29.md`,
finding SM-04).
**Category:** Data integrity / concurrent-edit safety.
**Description.** No entity in `CimsApp/Models/Entities.cs`
carries a rowversion / `IsConcurrencyToken` column. Two
concurrent edits of the same project / payment cert / CBS
line are silent last-write-wins — the loss is forensically
reconstructable via `AuditInterceptor` but the user
experience is silent data loss.

**Scope of work.**

- Add `[Timestamp] public byte[] RowVersion { get; set; }`
  (or EF-configured equivalent) to every entity with a UI
  edit path: Project, ProjectMember, CdeContainer, Document,
  DocumentRevision, Rfi, ActionItem, ProjectTemplate,
  CostBreakdownItem, Commitment, CostPeriod, ActualCost,
  Variation, PaymentCertificate.
- One migration adding the column to all of them.
- `ErrorHandlingMiddleware` maps
  `DbUpdateConcurrencyException` → 409 with a structured
  body describing the conflict.
- API responses surface the rowversion as an `ETag` header;
  PUT/PATCH callers send `If-Match`. Server compares; mismatch
  short-circuits to 409 before the DB write.
- UI-side conflict-resolution flow — at minimum a "this
  record changed since you opened it; reload?" dialog;
  ideally a side-by-side merge for high-value entities like
  PaymentCertificate.

**Estimate.** ~12-16h in a sprint.

**Why deferred.** v1.0 internal pilot has low concurrency;
the loss probability is tiny. Becomes load-bearing for v1.1
once concurrent users become a normal operational shape.
The audit-twin pattern's per-row Update capture means a
silently-overwritten edit IS reconstructable — so the
exposure is "user does work that vanishes" rather than "user
loses unrecoverable state". Acceptable trade for v1.0.

### B-025 — Notification feature implementation

**Source:** Post-S1 hardening pass (2026-04-29) — surfaced when
sweeping for dead entities.
**Category:** Product feature (UI / domain).
**Description.** A `Notification` entity exists in
`CimsApp/Models/Entities.cs` and was scaffolded into the schema
at `InitialCreate` (2026-04-18). It carries `UserId`, `Type`,
`Title`, `Body`, `Link`, `Read` / `ReadAt`, `CreatedAt`. The
`AuditInterceptor.SkippedEntityTypes` list includes it (with
the comment "UI-layer noise", suggesting the intent was a
high-volume UI notification stream that shouldn't pollute the
audit log).

**Status today.** Zero producers, zero consumers. The table is
defined, the DbSet exists, the tenant query filter is wired,
but no service writes to it and no controller serves it. It
has been dormant for the entire project lifetime.

**Scope of work to make it real.**

- A `NotificationService` with `CreateAsync(userId, type,
  title, body, link)` and `MarkReadAsync(notificationId,
  userId)` methods.
- Hook the create from the obvious producers — assigned an
  RFI / Action, mentioned in a comment, document transitions
  to a state the user should know about, payment cert issued
  to the user's org. Each producer needs explicit thinking
  about *should this notify*.
- A `NotificationsController` with `GET` (list, scoped to
  `User.Id == CurrentUserId`), `POST {id}/read`.
- Real-time delivery (SignalR? polling?) is a separate
  decision — v1.1 might ship polling-only.

**Why deferred.** v1.0 internal pilot does not need
notifications — a single internal user works through the UI
synchronously. The feature has real product surface area
(per-event notification policy, real-time delivery, read /
unread / dismissed lifecycle, mobile push later) that needs
its own sprint, not an opportunistic add.

**Recommendation when picked up.** Do NOT just delete the
entity to "clean up". The schema scaffold is harmless (empty
table) and re-adding the migration when the feature lands
creates churn. Leave dormant; pick it up as a real sprint.

### B-026 — `ProjectAppointment` / B2B contractor membership

**Source:** Post-S1 hardening pass (2026-04-29) — surfaced
alongside B-025.
**Category:** Multi-org collaboration (post-v1.0).
**Description.** A `ProjectAppointment` entity links a
non-AppointingParty Organisation to a Project — the original
intent (per the post-S1 secondary-mutation audit, SM-03's
"future expansion") was that v1.1 would let an
AppointingParty *appoint* a contractor org onto their
project, and users in the appointed org would gain project
access without being members of the AppointingParty
organisation.

**Status today.** Zero producers. `ProjectsService.GetByIdAsync`
loads `p.Appointments` via Include but the collection is
always empty. The entity, the FK relationships, the tenant
query filter, and the `Organisation.Appointments` /
`Project.Appointments` navigation properties all exist
unused.

**Scope of work to make it real.**

- A "create appointment" service method + controller endpoint:
  `POST /api/v1/projects/{projectId}/appointments` with body
  `{ organisationId, role, startDate?, endDate? }`. OrgAdmin
  of the AppointingParty (or SuperAdmin) only.
- Widen the B-023 / `AddMemberAsync` org-match check from
  "member's org == AppointingParty" to "member's org ==
  AppointingParty OR has an active ProjectAppointment on
  this project". The post-S1 secondary-mutation audit
  flagged this future widening explicitly.
- Tenant query filter widening: a user in an appointed org
  needs to *see* the project (currently the filter is
  `Project.AppointingPartyId == _tenant.OrganisationId`,
  which would hide it). Two-tenant visibility is the real
  architectural change — needs its own ADR.

**Why deferred.** v1.0 model is single-org-per-project per
ADR-0012 ("Project tenancy: caller's-org default with
SuperAdmin bypass"). Multi-org membership crosses an
ADR-level architectural boundary; an ADR is required to
update ADR-0012 (or supersede with a new ADR) before
implementation lands.

**Recommendation when picked up.** Same as B-025: leave the
schema dormant. The entity is part of the eventual
architecture; removing it would just create churn when the
feature lands.

### B-027 — SQL Server smoke test in CI / pre-merge

**Source:** Post-S1 hardening session 2026-04-29 — concrete
incident, not theoretical. The PR #41 bug
(`AuditLog.UserId` non-nullable + Guid.Empty sentinel for
anonymous flows = FK violation on real SQL Server) was 100%
latent under unit-test-only validation because the EF
in-memory provider ignores FK constraints. Was broken in
production for ~a week (since T-S0-11 / PR #11) before a
human-driven smoke test caught it.

**Category:** CI / quality gate.

**Description.** Add a CI job that boots the app against a
real SQL Server instance (LocalDB on Windows runners,
mssql/server in Docker on Linux runners), applies all
migrations, and exercises a small set of smoke endpoints:

- `POST /api/v1/organisations` (anonymous, returns 201 with
  bootstrap invitation) — covers the FK-permissive class of
  bug PR #41 closed.
- `POST /api/v1/auth/register` with the bootstrap token —
  covers the RegisterAsync transaction wrap (PR #29).
- `POST /api/v1/auth/login` with the new credentials — full
  auth path through SQL Server.
- `GET /api/v1/projects` — authenticated read with the
  tenant filter active.
- `POST /api/v1/projects` — authenticated write that goes
  through audit-twin atomicity (PR #33).

Each smoke test asserts the HTTP status and basic response
shape. Failures must block merge.

**Why deferred.** Setting up a CI job with a SQL Server
service is non-trivial — plus deciding the CI runner shape
(GitHub Actions Windows for LocalDB? Linux + mssql? both?).
Worth doing before any external customer deploy because the
in-memory test gap is not theoretical: it just bit us once.

**Promotion target (S5-retro action, 2026-05-01).** Five
sprints have now been retrospectively flagged for the missing
CI smoke step (S1, S2, S3, S4, S5). Pre-pilot is the firm
target — must land before the first external-customer deploy
of the v1.0 pilot. Honest non-target: cannot land mid-sprint
because the runner / Docker / LocalDB decision is itself a
Ch 17 ADR-shaped choice. Land as a dedicated infrastructure
sprint after S6 Procurement; refer to it as "the CI hardening
sprint" rather than scope-creep into another module.

**Until then:** treat the smoke-test-against-SQL-Server step
as the FIRST action of each sprint-close T-SX-13 task per the
S4-retro re-shaped rule. The S5 manual smoke-walk script
template lives at `docs/sprint-log/sN.md` daily entries and
is reusable — maintain it as the regression scaffold the
eventual CI job will replace.

### B-022 — Cache the `OnTokenValidated` user lookup

**Source:** Post-S1 auth-mutation audit
(`docs/security/post-s1-auth-mutation-audit-2026-04-28.md`,
finding AM-05).
**Category:** Performance / horizontal-scale readiness.
**Description.** `JwtBearerEvents.OnTokenValidated` does a
per-request `db.Users.FirstOrDefaultAsync` lookup to run
`TokenRevocation.IsRevoked`. Acceptable for the v1.0
single-instance deployment (PK lookup, fully indexed). Becomes
a hot path under horizontal scale (B-018 territory). Natural
fixes:

- Short-TTL in-memory cache (e.g. 30 s) per user, invalidated
  explicitly on `RevokeUserTokensAsync`.
- Per-user version stamp denormalised onto the JWT — the hook
  compares the stamp against the current User row, only doing
  the DB lookup when the stamps disagree.
- Distributed cache (paired with B-018's distributed
  `LoginAttemptTracker`).

**Why deferred.** Premature for v1.0 single-instance traffic.
Revisit alongside B-018 when scale-out is planned.

### B-012 — AI-assisted features in the product itself

**Source:** PAFM-SD Ch 3.4.
**Category:** Product.
**Description.** Embedding LLMs/AI features inside CIMS (document
summarisation, draft responses, anomaly detection) is v1.1+.
**Why deferred.** Anti-scope. AI-assisted *development* is fine and
in use; AI-assisted *product* is v1.1.

### B-028 — Schedule-side Monte Carlo simulation

**Source:** PAFM-SD `.docx` Appendix F.3; CR-004 deferred from S2.
**Category:** Risk simulation / S2 fast-follow OR v1.1.
**Description.** PAFM-SD Appendix F.3 names "Monte Carlo simulation
for schedule and cost contingency (at least 1000 iterations)". v1.0
ships the cost-side only (T-S2-08); the schedule-side is deferred.
Schedule contingency MC needs CPM activity data with duration
distributions per task and inter-task dependencies — which is the
core data structure of S4 Schedule & Programme. Until S4 lands,
schedule MC has no input data to simulate against.
**Why deferred.** Capacity (CR-004) plus a real upstream dependency
on S4. Re-evaluate at S4 close — by then the CPM data is in place
and the schedule MC pass becomes a small extension on top of the
cost-MC math T-S2-08 already shipped.

### B-029 — Opportunity register (mirror of Risk)

**Source:** PAFM-SD `.docx` Appendix F.3; CR-004 deferred from S2.
**Category:** Risk module completion.
**Description.** PAFM-SD F.3 closes with "Opportunity register with
the same structure" — a structural copy of the Risk register that
tracks positive risks (events that, if they occur, improve project
outcomes). Same RBS taxonomy, same 5×5 P×I matrix, same response
strategies (exploit / share / enhance / accept rather than the
threat-side avoid / transfer / mitigate / accept).
**Why deferred.** Mostly mechanical mirroring of Risk; doubles the
test surface for marginal v1.0 pilot value. Most UK construction
risk processes track threats (downside) far more rigorously than
opportunities (upside). Re-evaluate at first external customer who
manages positive risks formally.

### B-030 — Cross-module contingency drawdown link

**Source:** PAFM-SD `.docx` Appendix F.3 (drawdown tracking bullet);
CR-004 deferred from S2.
**Category:** Risk → Cost & Commercial integration.
**Description.** v1.0 (T-S2-09) tracks contingency drawdown amounts
per Risk via manual entry. The richer model links each drawdown
event to the specific Commitment / ActualCost / Variation that
consumed the contingency, giving a forensic chain from "risk
materialised" to "money spent". Crosses the Risk module / Cost &
Commercial module boundary; needs a service-layer integration
point and a UI that lets the user pick the consumer at drawdown
time.
**Why deferred.** Capacity plus a clean separation-of-concerns
benefit: v1.0 ships drawdown numbers honestly without coupling the
Risk module to S1's mutation surface. Re-evaluate when actual
construction projects start using the system and demand the
forensic chain (likely first external customer).

### B-031 — MS Project XML export

**Source:** PAFM-SD `.docx` Appendix F.5 (Schedule & Programme
"Import from and export to MS Project XML"); CR-005 deferred
from S4.
**Category:** Schedule & Programme interop.
**Description.** v1.0 (T-S4-09) ships MS Project XML *import*
only — read an existing schedule built in MS Project and bring
it into CIMS. Export reverses the mapping and emits the CIMS
schedule as MS Project-compatible XML, so users can round-trip
between the two tools. Implementation: walk Activities + their
computed CPM fields + Dependencies; emit `<Tasks>` and
`<PredecessorLink>` elements following the well-known core
shape of MSP `Project.xsd`.
**Why deferred.** Round-tripping back to MS Project is rarely
needed on day 1 of CIMS adoption: the overwhelming use case for
v1.0 is "I have an existing MS Project plan; let me get it into
CIMS so I can run my project there." Export becomes valuable
once a CIMS-resident schedule needs to feed downstream tools
that only consume MSP XML — a v1.1 customer-led requirement.

### B-032 — Takt planning for repetitive work

**Source:** PAFM-SD `.docx` Appendix F.5 ("Takt planning for
repetitive work"); CR-005 deferred from S4.
**Category:** Schedule & Programme — repetitive-work
optimisation.
**Description.** Takt planning is the lean-construction approach
for repetitive work (high-rise floors, infrastructure stations,
modular elements) where work is divided into equal-duration
"takt" blocks moving through location-based zones. Implementation
sketch (deferred from S4): `TaktZone` (location / area),
`TaktTrain` (a fixed-duration block), `TaktAssignment` linking
Activities to (Zone, Train).
**Why deferred.** Takt is a specialised optimisation that
matters for high-rise / infrastructure / modular projects but
is not used on typical UK construction projects (mid-rise
commercial, residential, fit-out). v1.0 ships the standard CPM
+ LPS workflow which covers >80% of the v1.0 pilot's expected
project types. Re-evaluate when a real customer with
repetitive-work projects engages.

### B-033 — Network-view (PERT diagram) data endpoint

**Source:** PAFM-SD `.docx` Appendix F.5 ("Gantt and network
views"); CR-005 deferred from S4.
**Category:** Schedule & Programme — visualisation.
**Description.** v1.0 ships Gantt only (T-S4-11). Network view
renders the dependency graph as a PERT-style node-and-arrow
diagram — useful for understanding the topology of a complex
schedule but rarely needed for day-to-day project management.
Implementation: `GET /schedules/network` returns nodes (one per
Activity) + edges (one per Dependency) in a shape suitable for
graph-rendering libraries (Cytoscape / vis.js / D3-force).
**Why deferred.** Gantt is the dominant schedule visualisation
in UK construction; the network view is a PMBOK-textbook
pedagogical tool that real PMs rarely consume. v1.1 candidate
for completeness once Gantt is in pilot use and feedback
surfaces a real need.

### B-034 — Hour as DurationUnit on Activity

**Source:** PAFM-SD F.5; `CimsApp/Models/Entities.cs` Activity
DurationUnit doc-comment (T-S4-02); rolled forward into the
backlog per the S4 retro action (T-S5-08).
**Category:** Schedule & Programme — sub-day scheduling.
**Description.** v1.0 only ships `DurationUnit.Day`; the enum
includes Hour as a value but the CPM solver and import paths
treat everything as Day. Honouring Hour requires the MSP
project calendar's working-hours-per-day setting (interlinked
with B-042). Activities like "Mobile crane lift" or "Concrete
pour" naturally measure in hours rather than fractional days,
so v1.1 customers with sub-day workflows will need this.
**Why deferred.** v1.0 pilot is mid-rise commercial — day-
granular scheduling is sufficient. Sub-day arithmetic also
needs calendar/working-hour modelling that doesn't exist in
v1.0. Land alongside B-042 in v1.1.

### B-035 — ChangeRequest structural impact links to Activity / CBS-line

**Source:** PAFM-SD F.6 fifth bullet ("Linked to variations,
programme impact, cost impact"); `docs/sprint-log/s5.md`
T-S5-06 notes; `CimsApp/Models/Entities.cs` ChangeRequest
ProgrammeImpactSummary / CostImpactSummary doc-comments.
**Category:** Change Control — structured impact tracking.
**Description.** v1.0 ships ProgrammeImpactSummary and
CostImpactSummary as free-text fields plus aggregate
EstimatedCostImpact / EstimatedTimeImpactDays decimals.
v1.1 adds structural links: `ChangeRequestActivityImpact`
(CR ↔ Activity, with delta-duration / delta-dates) and
`ChangeRequestCostLineImpact` (CR ↔ CostBreakdownItem,
with delta-budget). The Approve flow then knows exactly
which Activities to retime and which CBS lines to re-budget,
rather than relying on PM-typed prose.
**Why deferred.** Free-text is honest for the v1.0 pilot
where the PM is the audit trail; structural links require
deciding the cardinality (one CR → many Activities? one
Activity → many open CRs?) and the lifecycle (does Reject
clear the impact rows?). v1.1 candidate after pilot
feedback surfaces the real workflows.

### B-036 — Per-tenant approval delegation chains

**Source:** PAFM-SD F.6 fourth bullet ("Delegation-based
approval routing"); `docs/sprint-log/s5.md` T-S5-01 kickoff;
`CimsApp/Core/ChangeWorkflow.cs` doc-comments.
**Category:** Change Control / Admin Console.
**Description.** v1.0 hardcodes `ProjectManager+ approves`
for ChangeRequest. v1.1 introduces per-tenant delegation
rules: e.g. "Cat A scope changes route to OrgAdmin"; "Cost
impact ≥ £50k routes to Sponsor"; "Cat A BSA changes need
HSE-approver sign-off". Implementation: an `ApprovalRule`
entity per project (or per organisation) with predicates
on `Category` / `BsaCategory` / cost / time thresholds plus
target `UserRole` or `UserId`. ChangeRequestService.ApproveAsync
resolves the rule chain and rejects with `ForbiddenException`
if the caller doesn't match.
**Why deferred.** Genuine v1.1 — no v1.0 customer has yet
been able to articulate the delegation chains they want.
Lands alongside S14 Admin Console.

### B-037 — Per-tenant BSA HRB categorisation inference rules

**Source:** PAFM-SD F.6 third bullet; `CimsApp/Models/Entities.cs`
ChangeRequest.BsaCategory doc-comment; `docs/sprint-log/s5.md`
Top-3 risk #2.
**Category:** Change Control / Admin Console.
**Description.** v1.0 ships `BsaHrbCategory` as a manual tag
that the PM sets at raise / assess time. v1.1 adds an
inference engine: per-tenant rules ("any change touching
external cladding ≥ Cat A"; "any change adding occupant
load ≥ Cat B") that auto-infer the category from the CR's
description / linked Activities (B-035) / cost impact.
Reduces PM cognitive load on HRB projects where the BSA
categorisation is the most error-prone step.
**Why deferred.** Inference rules require a controlled
vocabulary (what counts as "cladding"?), which doesn't
exist in v1.0. Land alongside B-036 (per-tenant delegation)
since both share the rules-engine infrastructure.

### B-038 — NEC4 expedited-path for emergency ChangeRequests

**Source:** `CimsApp/Core/ChangeWorkflow.cs` doc-comments
(T-S5-03).
**Category:** Change Control — workflow extension.
**Description.** v1.0 enforces Raised → Assessed → Approved
strictly; emergency changes (e.g. site safety incident
requiring immediate work) cannot bypass the assess step.
v1.1 adds a "Project Manager's own assessment" expedited
path (mirrors NEC4 compensation event clause 64.4 PM
assessment): Raised → Approved direct with `ProjectManager+`
role + a mandatory `ExpeditedReason` field. Audit logs the
bypass distinctly. Implement is unchanged.
**Why deferred.** v1.0 pilot doesn't have any emergency
workflows yet; expedited path adds complexity that's only
value-adding when real urgency exists. Land at first
customer who hits an emergency.

### B-039 — Weekly Work Plan close-the-week with frozen PPC

**Source:** `CimsApp/Models/Entities.cs` WeeklyWorkPlan
doc-comments (T-S4-07); rolled forward per S4 retro action.
**Category:** Schedule & Programme — LPS lifecycle.
**Description.** v1.0 computes PPC on read every time
`GetWeeklyWorkPlanAsync` is called — never persisted. This
means PPC fluctuates if commitments are added / removed
post-week. v1.1 adds a `Close` transition that snapshots
the committed / completed counts and the resulting PPC
into a frozen state: `WeeklyWorkPlanState.Closed` with
`ClosedAt`, `ClosedById`, `FrozenPpc`. Once Closed, no new
commitments / completions can be recorded against the
WWP. Cumulative PPC across weeks (the PPC-trend view)
relies on Closed PPCs.
**Why deferred.** v1.0 pilot uses one PM tracking PPC
in real time; the read-time computation is correct for
that scenario. Cumulative trend reporting is the real
trigger for needing frozen PPC.

### B-040 — LPS per-tenant custom non-completion reasons

**Source:** `CimsApp/Models/Enums.cs`
LpsReasonForNonCompletion doc-comments (T-S4-07); rolled
forward per S4 retro action.
**Category:** Schedule & Programme — LPS configuration.
**Description.** v1.0 ships 8 reason categories
(ResourceUnavailability, MaterialDelay, WeatherImpact,
DesignChange, PrerequisiteIncomplete, ScopeChange,
AccessIssue, Other). UK construction projects often want
finer breakdowns: "Welfare facilities unavailable",
"Permit-to-work delay", "Adjacent operations preventing
access", "RAMS not approved". v1.1 adds a per-tenant
configurable list of reason categories (S14 Admin Console)
with per-project override. Existing rows keep their
v1.0 enum value — migration adds a `CustomReasonId`
nullable FK that takes precedence when set.
**Why deferred.** Per-tenant categories require S14
infrastructure (configurable controlled vocabularies).
v1.0 enum covers ~80% of real failure modes.

### B-041 — MS Project XML elapsed-days lag mode

**Source:** `CimsApp/Core/MsProjectXml.cs` doc-comments
(T-S4-09); rolled forward per S4 retro action.
**Category:** Schedule & Programme — MSP interop.
**Description.** v1.0 parses `LinkLag` as tenths-of-minutes
of working time (4800 tenth-minutes = 8h working-day = 1
working day). MS Project also supports "elapsed days"
lag mode — calendar days regardless of working calendar.
The `LagFormat` field on `PredecessorLink` distinguishes
the two; v1.0 ignores LagFormat and assumes working days.
v1.1 honours LagFormat and converts elapsed-days lag to
the appropriate decimal-days value (interlinked with B-042
project calendar).
**Why deferred.** Real exports rarely use elapsed-days lag
on UK construction projects (working-day lag is the
overwhelming default). v1.1 pilot-feedback driven.

### B-042 — Honour MSP project-calendar working-hours-per-day

**Source:** `CimsApp/Core/MsProjectXml.cs` doc-comments
(T-S4-09); `CimsApp/Models/Enums.cs` DurationUnit
doc-comments; rolled forward per S4 retro action.
**Category:** Schedule & Programme — MSP interop /
calendar modelling.
**Description.** v1.0 hardcodes 8 working hours per day
when converting MSP `Duration` (PT...H...M...S) to decimal
days. Real MSP projects can override via the project
calendar (some sites use 7.5h, 10h, 12h shifts). v1.1
parses the MSP `<Calendars>` block to extract the project
calendar's hours-per-day setting and uses it for both
Duration and LinkLag conversion. Interlinked with B-034
(Hour DurationUnit) — both hinge on calendar modelling.
**Why deferred.** Calendar modelling is a substantive sub-
project (working-day-of-week, holidays, exceptions);
v1.0 8h/day default covers ~90% of UK projects. Defer
until a customer with non-standard hours engages.

### B-043 — MSP XML fixture corpus expansion from real exports

**Source:** S4 retro "What did not go well" — test fixture
coverage one-sided.
**Category:** Schedule & Programme — MSP interop /
test coverage.
**Description.** v1.0 `MsProjectXmlTests` fixtures are all
hand-crafted XML snippets covering the textbook structure
(Tasks + PredecessorLinks). Real MSP exports include
features the v1.0 parser hasn't been stress-tested against:
outline hierarchy (parent / summary tasks), milestone rows
(Duration = PT0H0M0S), recurring tasks, calendar exceptions,
resource assignments, baseline copies, custom fields.
v1.1 candidate: as the first pilot customer imports their
real schedule, capture the XML as a fixture and add tests
for any new shapes encountered.
**Why deferred.** Speculative fixtures invite over-engineering
the parser for shapes that don't matter in practice. The
honest path is: ship v1.0, see what breaks at first real
import, write the fixture from the actual file.

### B-044 — EngagementLog full pagination

**Source:** `docs/sprint-log/s3.md` Top-3 risk #2;
`CimsApp/Services/Services.cs` ListEngagementsAsync
doc-comments; rolled forward per S4 retro action.
**Category:** Stakeholder & Communications — listing.
**Description.** v1.0 caps the engagement-log list at the
most-recent 200 entries per stakeholder. Long-running
projects with frequent stakeholder interactions can exceed
200 entries; v1.1 adds full pagination via `?cursor=...`
or `?since=...&until=...` parameters.
**Why deferred.** No v1.0 customer is yet expected to hit
the 200-entry cap; the cap is the simplest viable bound.

### B-045 — CommunicationItem Audience as Stakeholder structural link

**Source:** `docs/sprint-log/s3.md` kickoff Decisions
section; `CimsApp/Models/Entities.cs` CommunicationItem
Audience doc-comments.
**Category:** Stakeholder & Communications — structured
audience.
**Description.** v1.0 stores CommunicationItem.Audience as
free-text (e.g. "Client, PM, IM"). v1.1 introduces a join
table `CommunicationItemAudience` linking to specific
Stakeholder rows. Adds reporting capability ("show me
every communication that goes to Client X") and removes
the typo / inconsistency risk inherent in free text.
**Why deferred.** v1.0 pilot doesn't yet need the structured
queries the link enables; free text is the simpler v1.0
data shape and the v1.1 migration is mechanical (parse
existing free text into Stakeholder names where they match,
leave the rest as free text).

### B-046 — Org-level CommunicationItem templates

**Source:** `docs/sprint-log/s3.md` Top-3 risk #3;
`CimsApp/Models/Entities.cs` CommunicationItem doc-comments.
**Category:** Stakeholder & Communications / Admin Console.
**Description.** Every project repeats ~80% of the same
communications-matrix items (Monthly Project Report, Daily
Diary, Variation Notice, etc.). v1.1 adds org-level
`CommunicationItemTemplate` rows that seed new projects on
provisioning, similar to the existing `ProjectTemplate`
mechanism for documents. Per-tenant configurable; lives
in S14 Admin Console.
**Why deferred.** Per-project storage with the inevitable
duplication is honest for the v1.0 pilot (single org,
single project type). The template seeding is a customer-
onboarding optimisation that pays off at the second + third
customer, not the first.

### B-048 — NEC4 PM 4-week compensation-event notification deadline + deemed-acceptance

**Source:** PAFM-SD `.docx` Appendix F.7 (compensation events);
`docs/sprint-log/s6.md` T-S6-01 kickoff Top-3 risk #1;
`CimsApp/Core/CompensationEventWorkflow.cs` doc-comments
(T-S6-08).
**Category:** Procurement / NEC4 contract administration.
**Description.** NEC4 clause 61.3 requires the Project Manager
to respond to the contractor's compensation-event notification
within 4 weeks; failure to respond is deemed acceptance that the
event is a compensation event. v1.0 ships the bare 5-state
workflow without deadline-driven automatic transitions; v1.1
adds a scheduled job (or query-time computation) that:
(a) flags CEs in Notified state for >4 weeks; (b) on the deadline,
auto-transitions Notified → Quoted (or whatever NEC4 deems-CE
state means in CIMS terms) with an audit row noting the deemed
acceptance.
**Why deferred.** Deadline-driven automation requires a
scheduled-job framework (Quartz / Hangfire / ASP.NET background
service) — a substantive cross-cutting decision. v1.0 pilot
runs with manual deadline management.

### B-049 — NEC4 contractor 3-week quotation deadline + deemed-acceptance after PM non-response

**Source:** PAFM-SD `.docx` Appendix F.7;
`CimsApp/Core/CompensationEventWorkflow.cs` doc-comments
(T-S6-08).
**Category:** Procurement / NEC4 contract administration.
**Description.** NEC4 clause 62.3 requires the contractor to
submit a quotation within 3 weeks of being instructed; clause
62.6 invokes deemed acceptance if the PM doesn't respond to the
quotation within 2 weeks. Same deadline-driven automation
substrate as B-048; deferred together.
**Why deferred.** Deadline-driven automation infrastructure
deferred (see B-048). v1.0 manual.

### B-050 — NEC4 compensation-event risk-allowance pricing rules

**Source:** PAFM-SD `.docx` Appendix F.7;
`CimsApp/Models/Entities.cs` CompensationEvent
EstimatedCostImpact doc-comment (T-S6-08).
**Category:** Procurement / NEC4 quotation pricing.
**Description.** NEC4 clause 63 sets out how compensation-event
quotations should be priced — including a risk-allowance for
matters outside the contractor's control. v1.0 records the
quoted cost / time impact as free decimals without enforcing
or surfacing the risk-allowance breakdown. v1.1 adds:
(a) structured risk-allowance fields on the CompensationEvent
quotation; (b) optional integration with the S2 Risk register
to link the risk-allowance to specific Risk rows.
**Why deferred.** Risk-allowance modelling is a real construction-
contract domain question that wants a real customer's view of
how they want to track it. v1.1 customer-led.

### B-051 — Per-tenant evaluation criteria templates

**Source:** `docs/sprint-log/s6.md` T-S6-01 kickoff v1.1
candidates list; pre-flagged at S6 kickoff.
**Category:** Procurement / Admin Console.
**Description.** v1.0 requires criteria to be added per
TenderPackage individually. Most projects in a portfolio use
the same evaluation criteria pattern (price weight 60%, quality
breakdown weight 40% across consistent sub-criteria like
methodology / programme / sustainability / health-and-safety).
v1.1 adds per-tenant `EvaluationCriterionTemplate` rows that
seed new TenderPackages, mirroring the S3 `B-046`
CommunicationItemTemplate pattern.
**Why deferred.** Templates are an Admin-Console-shaped
optimisation. v1.0 records criteria correctly; v1.1 makes
recurring usage faster.

### B-052 — Multi-stage tendering (PQQ + ITT)

**Source:** `docs/sprint-log/s6.md` T-S6-01 kickoff v1.1
candidates list.
**Category:** Procurement / multi-stage workflow.
**Description.** Public-sector and large-private tendering
typically uses two stages: Pre-Qualification Questionnaire (PQQ)
to shortlist bidders, then Invitation to Tender (ITT) to the
shortlist. v1.0 ships single-stage tendering only (just the
ITT shape). v1.1 adds the PQQ entity with separate evaluation
criteria + a "promote to ITT" transition that copies the
shortlist of bidders onto a fresh TenderPackage.
**Why deferred.** Multi-stage adds substantial entity surface
and workflow complexity. v1.0 pilot at Genera Systems is
private-sector single-stage.

### B-053 — E-tendering vendor portal (subcontractor self-service bid submission)

**Source:** `docs/sprint-log/s6.md` T-S6-01 kickoff v1.1
candidates list.
**Category:** Procurement / external-user portal.
**Description.** v1.0 records bids that were collected
externally (email, paper, OJEU portal). v1.1 adds an external-
facing vendor portal where invited subcontractors can
self-service submit their tender against a TenderPackage,
with structured templates, required-field validation, and
audit trail. Implies SSO / federated identity for external
users — a bigger cross-cutting decision than the procurement
module on its own.
**Why deferred.** External-user identity is its own domain
(federated IdP, password policy, email verification, vendor
onboarding). v1.0 pilot doesn't need self-service tendering;
the PM admin enters bids on behalf of bidders.

### B-054 — Subcontractor pre-qualification register

**Source:** `docs/sprint-log/s6.md` T-S6-01 kickoff v1.1
candidates list; `CimsApp/Models/Entities.cs` Tender
BidderName doc-comment.
**Category:** Procurement / data normalisation.
**Description.** v1.0 stores BidderName + BidderOrganisation as
free-text on each Tender. v1.1 introduces a per-organisation
subcontractor register: `Bidder` entity with name +
organisation + contact details + PQQ status + insurance
documents + capability tags. Tender then references a Bidder
FK rather than free-text. Reduces typo / duplicate-organisation
churn across Tenders.
**Why deferred.** Free-text is honest for the v1.0 single-
project pilot. The pre-qualification register is a
multi-project / multi-customer optimisation; deserves the
S14-Admin-Console infrastructure to land first.

### B-055 — MPR PDF generation per PAFM Ch 30

**Source:** `docs/sprint-log/s7.md` kickoff PDF-rendering
decision (Option A); `CimsApp/Services/Services.cs`
`ReportingService` doc-comment; `CimsApp/DTOs/Dtos.cs`
`MprDto` doc-comment ("PAFM Ch 30 reference: best-effort
inference v1.0").
**Category:** Reporting — output rendering.
**Description.** v1.0 ships an MPR data aggregator returning
JSON only. v1.1 adds: (a) NuGet PDF library choice via
ADR-0015 — candidates QuestPDF (Community License — verify
Genera Systems still under the £1M revenue threshold at
ADR-write time), PdfSharpCore, or HTML→PDF (Puppeteer /
Playwright) — with the Ch 24.6 24-hour wait between adding
the package and exercising it; (b) PAFM Ch 30 canonical MPR
layout reconciled against the JSON DTO sections — the
v1.0 inference is Executive Summary / Programme / Cost /
Risk / Changes / Issues / Stakeholders, derived from
PMBOK 7 / NEC4 standard templates and may need re-shape
once the Ch 30 paste is supplied; (c) PDF endpoint at
`GET /api/v1/projects/{projectId}/reports/mpr.pdf`
returning `application/pdf`; (d) cover page + branding;
(e) per-section page breaks + tables with NEC4-flavoured
nomenclature.
**Why deferred.** Library-choice ADR + 24-hour wait would
have blocked T-S7-03 mid-sprint. The MPR data aggregation
is the genuinely-domain-shaped work; PDF templating is
mechanical once the library is chosen. Decoupling them
also lets the UI render the JSON DTO immediately (web
dashboards, .docx export, email digest, future BI tools).

### B-056 — Custom report cross-entity joins

**Source:** `docs/sprint-log/s7.md` kickoff v1.1 candidates;
T-S7-05 task description; `CimsApp/Core/CustomReportRunner.cs`
class doc-comment.
**Category:** Reporting — saved-query expressiveness.
**Description.** v1.0 ships single-entity custom reports
(pick one of Risk / ActionItem / Rfi / Variation /
ChangeRequest, filter, project columns). Real reporting
asks for joins — "all Actions on overdue RFIs", "all
Variations linked to Approved ChangeRequests", "all
Stakeholders mentioned in EngagementLogs in the last 30
days". v1.1 introduces a join model: secondary entity
type + join field + per-side filters / projections.
Likely shape: a `Joins[]` array in the persisted
definition, each entry a `(rightEntityType, joinKey,
filter, columns)` triple. Engine becomes a small
nested-loop / hash-join pass. Allow-list is per-
entity-pair to bound the surface.
**Why deferred.** Cross-entity joins are the v1.1
expressiveness story; v1.0 single-entity already covers
80% of "saved query" reporting needs.

### B-057 — Scheduled custom report runs + email distribution

**Source:** `docs/sprint-log/s7.md` kickoff v1.1 candidates;
T-S7-05 task description.
**Category:** Reporting — automation.
**Description.** v1.0 custom reports run on demand via
`POST /custom-reports/{id}/run`. v1.1 adds: (a) cron-shaped
schedule on each definition (daily / weekly / monthly with
caller-set time-of-day); (b) background runner pulling due
schedules and executing them; (c) email distribution to a
configurable address list (or `ProjectMembers` filter); (d)
report run history per definition (rows produced, runtime,
errors, recipients). Likely a hosted-service worker with a
new `CustomReportSchedule` + `CustomReportRun` entity pair.
**Why deferred.** Background workers + email are big
infrastructure; v1.0 pilot doesn't have ops cadence to
consume them.

### B-058 — Custom report exports (CSV / Excel / PDF)

**Source:** `docs/sprint-log/s7.md` kickoff v1.1 candidates;
T-S7-05 task description.
**Category:** Reporting — output formats.
**Description.** v1.0 custom reports return JSON. v1.1
adds query-string `format` selector or per-format
endpoints: `?format=csv` (RFC 4180), `?format=xlsx`
(EPPlus / OpenXmlSdk), `?format=pdf` (shares B-055's PDF
library choice). Same row data, different envelope.
**Why deferred.** Export formats are mechanical once the
v1.0 JSON aggregator + B-055 PDF library decision land.

### B-059 — HSE-specific dashboard cards

**Source:** `docs/sprint-log/s7.md` kickoff v1.1 candidates;
`CimsApp/Services/Services.cs` `DashboardsService.GetHseDashboardAsync`
doc-comment.
**Category:** Reporting / HSE — domain depth.
**Description.** v1.0 HSE dashboard ships a sparse single
"Coming in S12" placeholder card. S12 (Genera Systems QA,
HS&E Integration) lands the full HSE module — incidents,
near-misses, toolbox talks, RAMS, BSA-related controls.
v1.1 (or possibly direct in S12 close) adds HSE-specific
dashboard cards that aggregate the new entities: open
incidents, days-since-last-incident, overdue toolbox
talks, RAMS expiry warnings.
**Why deferred.** Sparse v1.0 placeholder is the honest
shape until S12 entities exist; HSE-specific cards land
when they have data to surface.

### B-060 — Custom report richer filter operators

**Source:** `docs/sprint-log/s7.md` T-S7-05 task; surface
through use; `CimsApp/Core/CustomReportRunner.cs` class
doc-comment.
**Category:** Reporting — saved-query expressiveness.
**Description.** v1.0 supports pure equality filters
(`{"Status": "Open"}`). v1.1 adds operator clauses:
`{"clauses":[{"field":"CreatedAt","op":"ge","value":"2026-01-01"},
{"field":"Score","op":"gt","value":15}]}`. Operator set:
eq, ne, gt, lt, ge, le, contains (string), in (list).
Same per-entity field allow-list governs which fields can
participate. Pushdown to SQL where feasible at the same
time (v1.0 in-memory eval is bounded by single-project
volume; richer operators benefit from index use).
**Why deferred.** Equality filtering covers the bulk of
"saved view of state" needs (open / approved / closed).
Date-range and threshold filtering becomes valuable once
the pilot has > 6 months of history.

### B-047 — Explicit per-stakeholder EngagementPlan entity

**Source:** `docs/sprint-log/s3.md` kickoff Decisions
section; `CimsApp/Models/Entities.cs` Stakeholder
EngagementApproach + EngagementNotes doc-comments;
T-S3-05 task description.
**Category:** Stakeholder & Communications — structured
engagement plans.
**Description.** v1.0 models the engagement plan as two
fields on Stakeholder: `EngagementApproach` (Mendelow
quadrant enum) + `EngagementNotes` (free text). v1.1 adds
a structured `EngagementPlan` entity with: planned
interactions per cadence, target outcomes, success metrics,
linked CommunicationItems, owner per touchpoint. Stakeholder
becomes the parent; multiple historical plans per stakeholder
allowed (re-plan as projects evolve).
**Why deferred.** Real workflows seem to want approach
+ free-text plan; structured per-touchpoint planning is
heavier than v1.0 customers will set up. Re-evaluate at
first pilot if the free-text plan is being copy-pasted
across stakeholders.

### B-061 — Code coverage reporting in CI

**Source:** `docs/sprint-log/s8.md` kickoff v1.1 candidates;
T-S8-06 task.
**Category:** CI / quality gate.
**Description.** v1.0 ships build / test / SQL smoke /
migration round-trip CI jobs. v1.1 adds line + branch
coverage via Coverlet (`dotnet test --collect:"XPlat Code
Coverage"`), uploads to Codecov or a similar service, and
gates the PR check on a coverage delta budget (e.g. "no PR
may drop coverage by more than 1.0 percentage point"). Most
useful once the codebase has settled enough that the
coverage number reflects intent rather than churn.
**Why deferred.** Coverage gates are noisy mid-development;
S0..S7 added 808 tests against a moving target. Adding
coverage gates now would surface false positives every time
a sprint's first commits land before the matching tests do.

### B-062 — Performance smoke / latency budgets

**Source:** `docs/sprint-log/s8.md` kickoff v1.1 candidates;
T-S8-03 task.
**Category:** CI / quality gate.
**Description.** v1.0 SQL smoke asserts only HTTP status +
shape. v1.1 adds per-endpoint latency budgets (e.g. `GET
/projects` p99 < 200ms, `POST /projects` p99 < 500ms),
running each endpoint N times and asserting against
percentile budgets. Catches regression-shaped perf bugs
(an N+1 in `/projects` list, a missing index on the audit
read path) without needing a full load-test rig.
**Why deferred.** Latency is highly variable on shared
GitHub Actions runners; the noise floor would require
generous budgets that wouldn't catch real regressions
until pilot-volume data exists.

### B-063 — Security smoke (CVE scan + ZAP baseline)

**Source:** `docs/sprint-log/s8.md` kickoff v1.1 candidates;
T-S8-06 task.
**Category:** CI / security gate.
**Description.** v1.0 CI does not scan dependencies or run
security probes. v1.1 adds: (a) `dotnet list package
--vulnerable --include-transitive` step that fails on any
known CVE; (b) Dependabot / Renovate enabled on the repo so
upgrade PRs land for new advisories; (c) OWASP ZAP baseline
scan against the running app in the SQL smoke job (passive
checks only — `zap-baseline.py`). All three are low-effort
adds that catch a class of issues unit tests cannot.
**Why deferred.** Each layer needs its own ADR-shaped
configuration call (ignore-list for known false positives,
severity threshold for fail/warn, schedule for daily vs
per-PR). Worth doing before pilot but not in S8's already-
full scope.

### B-064 — Pre-commit hooks for the local dev loop

**Source:** `docs/sprint-log/s8.md` kickoff v1.1 candidates;
T-S8-06 task.
**Category:** Developer experience.
**Description.** v1.0 catches all issues at CI-time (push or
PR). v1.1 adds local pre-commit hooks via Husky.NET (or
git's native hooks): build + test + format on every commit.
Trade-off is faster feedback (~30s before commit lands
locally) vs slower commits. Most useful in multi-developer
teams; less essential for a solo dev whose CI loop is
already tight.
**Why deferred.** Solo-dev-friendly hook setup is a v1.1
ergonomics improvement, not a v1.0 deployability gate.

### B-065 — ISO 19650 reference data per-tenant overrides

**Source:** `docs/sprint-log/s9.md` kickoff v1.1 candidates;
`CimsApp/Core/Iso19650Codes.cs` doc-comment.
**Category:** Information Management — per-tenant config.
**Description.** v1.0 ships the standard ISO 19650-2 Annex A
codes hard-coded in `Core/Iso19650Codes.cs`. Real projects
often add custom Volume codes (e.g. "PHASE1A", "TWR-N") or
restrict allowed Roles to actually-appointed firms. v1.1
introduces per-organisation overrides via the S14 Admin
Console; validator runs against the merged set per request.
**Why deferred.** Needs the S14 Admin Console; multi-
customer optimisation that doesn't unblock the v1.0 pilot.

### B-066 — MIDP / TIDP gantt timeline visualisation

**Source:** `docs/sprint-log/s9.md` kickoff v1.1 candidates.
**Category:** Information Management — UI.
**Description.** v1.0 ships MIDP / TIDP as JSON list
endpoints; v1.1 adds a gantt / timeline view per project —
master row of MIDP entries with each team's TIDP entries
grouped beneath, completion + sign-off colour-coded. S15
Mobile Views or earlier polish in S14.
**Why deferred.** Data-shape ships in v1.0; UI is a fast-
follower.

### B-067 — TIDP auto-generation from project template

**Source:** `docs/sprint-log/s9.md` kickoff v1.1 candidates.
**Category:** Information Management — onboarding speed.
**Description.** v1.0 requires manual TIDP creation per team
per MidpEntry. v1.1 adds template-driven flow: project from
`ProjectTemplate` (S0) carries a default MIDP + per-team
TIDP skeleton; new project gets it populated automatically.
**Why deferred.** Templating in v1.0 is intentionally
minimal; template-driven generation is a multi-customer
optimisation.

### B-068 — NBS-backed Uniclass / IFC classification feed

**Source:** `docs/sprint-log/s9.md` kickoff v1.1 candidates;
`CimsApp/Core/Iso19650Codes.cs` Uniclass/IFC doc-comments;
`CimsApp/Services/Iso19650/Iso19650FilenameValidator.cs`
checks 7-10 deferred-stub messages.
**Category:** Information Management — classification.
**Description.** v1.0 ships the validator with checks 1-6
active; checks 7-10 (Uniclass mandatory / Uniclass
deprecation / IFC schema / cross-reference integrity) all
run against hard-coded illustrative maps. v1.1 replaces them
with an authoritative NBS-backed feed (live or pinned-offline
+ scheduled refresh). Checks 7-10 then become genuinely
meaningful gates.
**Why deferred.** Needs ADR-shaped choice (live vs pinned,
refresh cadence, cost) plus real-customer feedback on
which classifications they use.

### B-069 — Per-team Team entity for TIDP

**Source:** `docs/sprint-log/s9.md` kickoff v1.1 candidates;
`CimsApp/Models/Entities.cs` TidpEntry.TeamName doc-comment.
**Category:** Information Management — structured teams.
**Description.** v1.0 stores TIDP team identity as a free-
text `TeamName`. v1.1 introduces a structured `Team` entity
(per-project: members, lead, discipline, deliverable count)
that TidpEntry references via FK. Adds reporting ("all of
Team Architecture's deliverables across all MIDPs") and
removes typo risk.
**Why deferred.** v1.0 pilot operates with 2-3 teams;
structured Team doubles the F.9 entity surface without
proportional pilot value.

### B-070 — BSR API integration for MOR push

**Source:** `docs/sprint-log/s10.md` kickoff v1.1 candidates;
T-S10-04 task; `CimsApp/Models/Entities.cs`
MandatoryOccurrenceReport doc-comment.
**Category:** BSA 2022 / regulatory integration.
**Description.** v1.0 captures MOR data + a manual `ReportedToBsr`
flag pair. The Building Safety Regulator currently has no
public push API (verify at v1.1 implementation time). When
a BSR API exists, v1.1 wires up programmatic push: on
mark-reported the service POSTs to BSR, captures the
returned case reference, and surfaces push errors. Until
then, v1.0's manual flag is the honest shape.
**Why deferred.** No public BSR API to integrate with.

### B-071 — Safety Case Summary canonical Schedule 5 + PDF

**Source:** `docs/sprint-log/s10.md` kickoff v1.1 candidates;
`CimsApp/Services/Services.cs` SafetyCaseService doc-comment.
**Category:** BSA 2022 / output rendering.
**Description.** v1.0 ships SafetyCaseSummary as a JSON
aggregator with a best-effort inference of BSA 2022
Schedule 5 structure. v1.1 reconciles the section list +
field labels against canonical Schedule 5 wording from
PAFM-CIMS-001 PDF + adds PDF generation (shares the B-055
PDF library decision). Pre-pilot legal review is a separate
step that may further refine the field shape.
**Why deferred.** Canonical Schedule 5 wording is paste-on-
request from PAFM-CIMS-001 PDF; legal review is a pre-pilot
step not a v1.0 deliverable.

### B-072 — Per-tenant HRB inference rules

**Source:** `docs/sprint-log/s10.md` kickoff v1.1 candidates;
`CimsApp/Models/Entities.cs` Project.IsHrb / HrbCategory
doc-comments.
**Category:** BSA 2022 / per-tenant config.
**Description.** v1.0 PMs manually toggle IsHrb and pick
HrbCategory. v1.1 adds an inference-rule engine in S14
Admin Console: each tenant defines rules ("residential ≥ 18m
→ HRB cat A", "mixed-use ≥ 7 storeys → HRB cat B"). Inferred
category is suggested at project create + flagged when PM
override diverges.
**Why deferred.** Needs S14 Admin Console; multi-customer
optimisation; v1.0 manual toggle is honest for the single
pilot.

### B-073 — Gateway package automatic deadline tracking

**Source:** `docs/sprint-log/s10.md` kickoff v1.1 candidates;
T-S10-03 task.
**Category:** BSA 2022 / statutory deadlines.
**Description.** BSA 2022 has decision-deadline statutory
windows for each Gateway. v1.0 records SubmittedAt and
DecidedAt but doesn't auto-flag overdue submissions. v1.1
adds: (a) per-Type deadline configuration; (b) overdue
flag on the GatewayPackage row; (c) overdue notifications
via the S13 notifications module.
**Why deferred.** Deadline rules need Schedule reconciliation
+ S13 notifications infrastructure.

### B-074 — Audit-log query layer for Golden Thread compliance

**Source:** `docs/sprint-log/s10.md` kickoff v1.1 candidates;
T-S10-06 task.
**Category:** BSA 2022 / compliance reporting.
**Description.** v1.0 marks Documents as IsInGoldenThread;
the existing AuditInterceptor + AuditLog already capture
per-row before/after history. v1.1 adds a structured query
endpoint surfacing the audit trail for GT-marked documents
("show me every change made to Golden Thread documents in
date range X" with pagination + filtering by user / action
type). Powers compliance evidence packs at building
handover.
**Why deferred.** Query-layer surface is wide; needs
real-customer requirements input on what compliance shape
they want first.

### B-075 — Golden Thread soft-immutability un-mark workflow

**Source:** `docs/sprint-log/s10.md` kickoff v1.1 candidates;
`CimsApp/Models/Entities.cs` Document.IsInGoldenThread
doc-comment.
**Category:** BSA 2022 / Golden Thread.
**Description.** v1.0 mark-into-GT is one-way to keep the
immutability claim honest. v1.1 adds a multi-step un-mark
workflow with a written justification, dual approval (IM +
PM), and full audit. Rare flow — only when a document was
genuinely added to the GT in error (typo / wrong document
selected). The audit trail captures the un-mark with the
justification so compliance reviewers can verify.
**Why deferred.** v1.0 simpler; un-mark is a corner case
that needs careful design to avoid weakening the GT claim.

### B-076 — ICO breach reporting API integration

**Source:** `docs/sprint-log/s11.md` kickoff v1.1 candidates;
T-S11-05 task; `CimsApp/Models/Entities.cs` DataBreachLog
doc-comment.
**Category:** UK GDPR / regulatory integration.
**Description.** v1.0 captures breach data + a manual
ReportedToIco flag pair. The ICO currently does not publish
a programmatic push API (verify at v1.1 implementation
time). When the API exists, v1.1 wires up the push: on
mark-reported the service POSTs to ICO, captures the
returned case reference, surfaces push errors. Same shape
as B-070 (BSR API for MOR). // GDPR ref: Art. 33.
**Why deferred.** No public ICO API to integrate with at
implementation time.

### B-077 — SAR auto-extraction across domain entities

**Source:** `docs/sprint-log/s11.md` kickoff v1.1 candidates;
T-S11-04 task.
**Category:** UK GDPR / SAR fulfilment automation.
**Description.** v1.0 captures SAR intake + manual
fulfilment by IM / DPO. v1.1 adds per-domain auto-
extractors: User profile, ProjectMembers (where the data
subject is a project user), AuditLogs containing the
subject's UserId, Documents authored by the subject, RFIs /
ChangeRequests / EngagementLogs raised, etc. Composes into
a single SAR fulfilment package (JSON / ZIP export per
B-NNN). // GDPR ref: Art. 15.
**Why deferred.** Per-domain extractors are a wide cross-
cutting feature; pilot scope doesn't yet have customer
data shape requirements that drive the package format.

### B-078 — Retention auto-enforcement across domains

**Source:** `docs/sprint-log/s11.md` kickoff v1.1 candidates;
T-S11-06 task.
**Category:** UK GDPR / retention automation.
**Description.** v1.0 stores RetentionSchedule entries as
reference data only. v1.1 wires retention-schedule expiry
to per-domain deletion / archival triggers (User soft-
delete, Project archival, Document voiding, AuditLog
retention windows). Cross-domain deletion ordering needs
an ADR (e.g. an AuditLog row about a User that's been
retention-deleted has separate retention rules). // GDPR
ref: Art. 5(1)(e).
**Why deferred.** Touches every domain; ADR + customer
input on retention shapes needed first.

### B-079 — DPIA structured ICO questionnaire schema

**Source:** `docs/sprint-log/s11.md` kickoff v1.1 candidates;
T-S11-03 task; `CimsApp/Models/Entities.cs`
DataProtectionImpactAssessment doc-comment.
**Category:** UK GDPR / DPIA shape.
**Description.** v1.0 ships free-text Description +
HighRiskProcessingDescription + MitigationsDescription
fields. v1.1 reconciles against the canonical ICO DPIA
template (structured questions per Art. 35(7) sub-paragraphs:
nature/scope/context/purposes; necessity/proportionality;
risks to rights/freedoms; measures to address risks).
Pre-pilot legal review on the field shape. // GDPR ref:
Art. 35(7).
**Why deferred.** Canonical questionnaire wording is
paste-on-request from PAFM-CIMS-001 PDF; legal review is
a pre-pilot step.

### B-080 — Per-tenant retention schedule templates

**Source:** `docs/sprint-log/s11.md` kickoff v1.1 candidates;
T-S11-06 task.
**Category:** UK GDPR / onboarding speed.
**Description.** v1.0 each org defines retention schedules
manually. v1.1 ships pre-baked templates for common UK
construction profiles (residential development /
commercial / public-sector / contracts under the
Construction Act 1996). New orgs apply a template at
sign-up + customise. S14 Admin Console territory.
**Why deferred.** Multi-customer optimisation; v1.0 manual
entry honest for the single pilot.

### B-081 — PDCA cycle history per Improvement

**Source:** `docs/sprint-log/s12.md` kickoff v1.1 candidates;
T-S12-02 task; `CimsApp/Models/Entities.cs`
ImprovementRegisterEntry doc-comment.
**Category:** Kaizen / per-cycle audit.
**Description.** v1.0 stores per-stage notes (PlanNotes,
DoNotes, CheckNotes, ActNotes) on the Improvement entry.
On Act → Plan cycle-back, CycleNumber increments and the
per-stage notes reset (current cycle data lost). v1.1 adds
a `PdcaCycleHistory` row per completed cycle, snapshotting
the prior cycle's notes so multi-iteration improvements
show their full PDCA trail.
**Why deferred.** v1.0 simple; per-cycle history adds an
entity for what is currently expressible as a free-text
"see prior notes" field if needed.

### B-082 — Lessons-learned tagging + full-text search

**Source:** `docs/sprint-log/s12.md` kickoff v1.1 candidates;
T-S12-03 task; `CimsApp/Models/Entities.cs` LessonLearned
TagsCsv doc-comment.
**Category:** Kaizen / lessons library expressiveness.
**Description.** v1.0 ships free-text Category +
comma-separated TagsCsv. v1.1 introduces a structured tag
taxonomy (per-tenant) + full-text search across the
lessons library (Title + Description + tags). Powers a
"find lessons relevant to my situation" surface.
**Why deferred.** v1.0 simple list with optional category
+ tag filter is enough for a starter library; full-text
needs DB-side support (SQL Server FTS is straightforward
but not free).

### B-083 — OpportunityToImprove polymorphic source FK

**Source:** `docs/sprint-log/s12.md` kickoff v1.1 candidates;
T-S12-04 task; `CimsApp/Models/Entities.cs`
OpportunityToImprove SourceEntityType doc-comment.
**Category:** Kaizen / Opportunity navigability.
**Description.** v1.0 stores SourceEntityType (string,
service-normalised PascalCase) + SourceEntityId (Guid?) as
a JSON metadata pointer. v1.1 introduces per-domain join
tables (`OpportunityToImproveOnRisk`,
`OpportunityToImproveOnRfi`, etc.) that give true
navigability — "show me all Opportunities raised against
this Risk". The trade-off is a wider entity surface.
**Why deferred.** v1.0 free-text is enough to record where
the opportunity was spotted; navigability is a "real
workflow has emerged" v1.1 ask.

### B-084 — Improvement-register dashboard / KPI integration

**Source:** `docs/sprint-log/s12.md` kickoff v1.1 candidates.
**Category:** Kaizen / surfacing.
**Description.** v1.0 ships the Improvement / Lesson /
Opportunity data; v1.1 adds dashboards re-using the S7
DashboardsService pattern (cards: open improvements per
PDCA stage, average cycle time, closed-vs-cycled-back
ratio, top tagged categories).
**Why deferred.** Data surface is the v1.0 contract; the
dashboard is a fast-follower once pilot users have
populated the register.

### B-085 — Lessons-library template seed

**Source:** `docs/sprint-log/s12.md` kickoff v1.1 candidates.
**Category:** Kaizen / onboarding.
**Description.** v1.0 lessons library starts empty per org.
v1.1 ships pre-baked common UK construction lessons (e.g.
"verify ground conditions before piling", "early-warning
on long-lead-time materials") as starter content. Per-
tenant template applied at onboarding; users curate +
extend from there. S14 Admin Console territory.
**Why deferred.** Multi-customer optimisation; v1.0 starts-
empty is honest for the single pilot.

### B-086 — Genera Systems REST API endpoints per PAFM Ch 47

**Source:** `docs/sprint-log/s13.md` kickoff Option A scope
cut; `CimsApp/Models/Entities.cs` InspectionActivity
doc-comment.
**Category:** F.13 Genera Integration.
**Description.** v1.0 ships only the CIMS-side
InspectionActivity entity + manual-entry endpoints. v1.1
adds the REST API endpoints per PAFM Ch 47 — the canonical
Genera Systems QA / HSE integration specification — that
let Genera-side systems read / write inspection activities,
photos, outcomes, and inspection-type taxonomies.
**Unblock condition.** PAFM Ch 47 paste from PAFM-CIMS-001
PDF + Genera-side API spec confirmation.
**Why deferred.** Ch 47 not yet supplied. Genera Systems
QA/HSE platform is also under development (Genera Systems
is the user's own company); the spec may still be evolving.

### B-087 — Webhook subscription for QA / HSE events

**Source:** `docs/sprint-log/s13.md` kickoff Option A scope
cut.
**Category:** F.13 Genera Integration.
**Description.** v1.1 adds CIMS-side webhook subscription
for Genera-pushed events: inspection scheduled, started,
completed, cancelled, photo uploaded, etc. Wires up
through B-086's API surface; receives pushes from
Genera and updates corresponding CIMS InspectionActivity
rows. Conflict resolution rules land in B-089.
**Unblock condition.** Ch 47 webhook event schema +
delivery-semantics decision (at-least-once vs
exactly-once).
**Why deferred.** Depends on B-086.

### B-088 — Identity sync / shared SSO with Genera

**Source:** `docs/sprint-log/s13.md` kickoff Option A scope
cut.
**Category:** F.13 Genera Integration.
**Description.** Either Genera and CIMS share an external
IdP (SAML / OIDC; e.g. Microsoft Entra ID) or one accepts
the other's tokens with a trust relationship. Replaces
the v1.0 CIMS-only JWT issuance for users who also exist
on the Genera side.
**Unblock condition.** Genera IdP decision (which IdP?
SAML or OIDC? federation or token-exchange?).
**Why deferred.** Genera-side IdP decision not yet made.

### B-089 — Bidirectional sync of inspection activities

**Source:** `docs/sprint-log/s13.md` kickoff Option A scope
cut.
**Category:** F.13 Genera Integration.
**Description.** Builds on B-086 + B-087. CIMS-side
InspectionActivity row creation / updates push to Genera;
Genera-side equivalents push to CIMS via webhook. Conflict
resolution rules — typically last-writer-wins per field,
with a CIMS-authoritative override flag for project-
specific overrides. Ownership decisions land here:
- Inspection scheduling: Genera or CIMS authoritative?
- Inspection outcome: who's the source of truth?
- Photo evidence storage: which side stores blobs?
**Unblock condition.** Ch 47 + B-086 + B-087 + Genera-side
data-ownership decisions.
**Why deferred.** Foundation tasks (B-086..088) come first.

## Source legend

- `SR-S0-XX` — Sprint 0 security review finding
  (`docs/security/s0-review-2026-04-24.md`).
- `CR-XXX` — Change register entry (`docs/change-register.md`).
- Role matrix — `docs/security/role-matrix.md`.
- PAFM-SD Ch X.Y — software development discipline manual,
  `Notes/PAFM_Software_Development_v2.0.docx`.
